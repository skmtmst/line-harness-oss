-- フォルダに色を持たせる。
--
-- これまで色はタグ側（tags.color）にあった。タグ1つずつに色を決めさせると、
-- 100枚あるタグで色がばらけて、一覧を見ても区別に使えない。分類（フォルダ）に
-- 色を付けて、そこに属するタグが同じ色になる形にする。
--
-- ALTER は1文だけにする。D1 は1ファイルに複数の ALTER を入れると途中で落ちる
-- ことがあり、_migrations に記録も残らない（113 で踏んだ）。
ALTER TABLE folders ADD COLUMN color TEXT;
