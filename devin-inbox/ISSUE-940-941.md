# ISSUE #940・#941 — 外部連携の残り2クラスタ

作業ブランチ: `devin/940-external-envelope`（base: `codex/development` @ `1d8f31da0`）

## Issue #940 — 送信の封筒・署名・照合の統一

| 指摘 | 対応 | 主な変更箇所 |
| --- | --- | --- |
| N-371 封筒が経路ごとに違う | 共通封筒 `buildOutgoingWebhookBody`（`{id,type,occurred_at,account_id,data,attempt}`）を通常配送・V6自動処理・旧式直書きURL・試し送信・送り直しの全経路で使用 | `apps/worker/src/services/outgoing-webhook-delivery.ts`、`event-bus.ts`、`automation-action-executors.ts`、`routes/webhooks.ts` |
| N-372 署名・冪等ヘッダが経路ごとに違う | `X-Harness-Event-Id` / `X-Harness-Timestamp` / `X-Harness-Signature: v1=<hex>` に統一。署名は `operations-signature.ts` の HMAC-SHA256（`timestamp.eventId.body`）に集約。冪等キー＝封筒の `id` | 同上 + `operations-signature.ts` |
| N-376 壊れた event_types が全体を止める | `getActiveOutgoingWebhooksByEvent` を行ごとの JSON 解析に変更。壊れた行だけ構造化ログを出して飛ばす | `packages/db/src/webhooks.ts` |
| N-377 検証済みメアド・電話の裏付け | `verified_email`/`verified_phone` は `user_profile_values.verified_at` が立つ採用値だけに一致。`users.email`/`users.phone` の生の値では結び付けない | `apps/worker/src/services/incoming-webhook-actions.ts` |

## Issue #941 — 設定画面の操作・表・一括・試し送信

| 指摘 | 対応 | 主な変更箇所 |
| --- | --- | --- |
| N-385 設定メニューがマウス専用 | `role="menu"`/`menuitem` 化、開くと最初の項目へフォーカス、矢印・Home/End で移動、Escape で閉じて「設定」ボタンへ戻る、外側 pointerdown で閉じる（共有部品は触らず、当該画面の独自メニューに揃えた） | `apps/web/src/app/webhooks/webhook-overviews.tsx` |
| N-386 6列の表が狭い幅で溢れる | 1180px以下で「かかった時間」、760px以下でさらに「送った・届いた中身」を非表示。隠した値は「中身を見る」の詳細で確認可能 | `webhook-interactions.tsx` / `.module.css` |
| N-387 一括再試行が5件以降を黙って残す | 応答に `remaining`（今回試行しなかった残件数）を追加し、画面に残件数と「もう一度押すと続きをやり直す」を表示 | `packages/db/src/webhooks.ts`、`routes/webhooks.ts`、`webhook-interactions.tsx`、`lib/api.ts` |
| N-388 試し送信が確認なしに実URLへ届く | 「1回 試してみる」→ `ConfirmDialog` で実URLを省略せず表示 → 確認後に送信 | `webhook-overviews.tsx` |

## 検証

- `apps/worker`: webhooks系テスト計 300+ 件 pass（envelope/署名ヘッダ・N-377 照合・retry remaining・旧経路を含む）
- `packages/db`: `outgoing-webhook-scope.test.ts` / `webhooks-external-integrations.test.ts` 18件 pass
- `apps/web`: webhooks 契約テスト 69件 pass（新規 `webhook-n940-941-contract.test.ts` 含む）
- 型検査: `packages/db` / `apps/worker` / `apps/web` すべて pass

## 未検証（要フォロー）

- V6 の視覚一致証拠（Pencil 実ノード ID・設計画像・実装スクリーンショットの目視比較）は未取得。PR の Visual Parity 欄は `unverified` とする。

## required gate 差し戻し対応（2026-09-20）

PR #357 が必須ゲートで2件落ちた。いずれも本実装の副作用で、通常commitで修正。

1. **`mileage-reward-delivery-d1.test.ts`**（`expected 'object' to be 'string'`）
   - 原因: マイレージ交換の外部送信は V6 自動処理の `webhookExecutor` を経由するため、N-372 のヘッダ統一で冪等キーが `Idempotency-Key` → `X-Harness-Event-Id` に移った。テストの fetch カウンタが旧ヘッダを読み `null`（object）を拾っていた。
   - 対応: 仕様（封筒id＝冪等キー＝X-Harness-Event-Id）は意図どおりなので、テスト側の読み取りヘッダを新契約へ更新。実装は変えていない。
   - 確認: `mileage-reward-delivery.test.ts` + `mileage-reward-delivery-d1.test.ts` 23件 pass。

2. **`design-debt.test.ts`**（`unresolved-classname` 29 → 35）
   - 原因: N-386 の列畳みで `className={styles.durationCol}` 等の動的 className を6箇所追加し、静的解析できない直書きとして検出された。
   - 対応: className を増やさず、CSS の `nth-child` セレクタ（このファイルの列幅指定と同じ作法）で見出し・列定義・セルを一括に畳む形へ変更。baseline は書き換えていない。
   - 確認: `design-debt.test.ts` 12件 pass、`apps/web/src/app/webhooks` 契約テスト 69件 pass（N-386 の契約テストも nth-child 版へ更新）。
