# ISSUE #950・#954・#956・#957 — 小粒の監査項目4件

作業ブランチ: `devin/950-small-fixes`（base: `codex/development` @ `1d8f31da0`）
PR: https://github.com/skmtmst/line-harness-oss/pull/358

## Issue #950 — ダッシュボード（N-008 / N-009 / N-010 / N-013）

| 指摘 | 対応 | 主な変更箇所 |
| --- | --- | --- |
| N-008 「取得できません」「読み込み中」など禁止文言が残る | 画面ごとの直書きをやめ、共通の `STATE_TEXT`（読み込み中・失敗の定型文）へ統一。出荷予定パネル・未対応カード・通知一覧も同じ文言に揃えた | `apps/web/src/app/page.tsx`、`apps/web/src/components/dashboard/shipment-panel.tsx`、`apps/web/src/components/support/pending-inbox-card.tsx`、`apps/web/src/app/notifications/page.tsx` |
| N-009 「すべての通知を見る →」が100件までのパネルしか見せない | 実ページ `/notifications` を新設。種類（すべて／エラー／アップデート）を URL `?category=` で保持し、「さらに読み込む」で offset ページネーション。行を押すと既読にして行き先へ送る動きはパネルと同じ判定を共有 | `apps/web/src/app/notifications/page.tsx`、`apps/web/src/components/dashboard/notification-summary.ts`、`apps/worker/src/routes/notification-center.ts`、`packages/db/src/notifications.ts`、`apps/web/src/lib/api.ts` |
| N-010 期間・編集・QR ダイアログの状態がリロードで消える | 期間 `?period=`・友だち編集 `?edit=`・QR `?qr=1` を URL クエリへ保持。再読み込み・共有URLでも同じ状態で開く | `apps/web/src/app/page.tsx`、`apps/web/src/components/dashboard/qr-dialog.tsx` |
| N-013 経路不明が普通の経路名として出る | 既知の経路名にない値は「不明」として分離し、空の内訳（経路なし）と区別。不明分は経路名として並べず中立の表示にする | `packages/db/src/dashboard.ts`、`apps/web/src/components/dashboard/friend-trend-table.tsx` |

## Issue #954 — 受信トレイの保存検索（N-020）

| 指摘 | 対応 | 主な変更箇所 |
| --- | --- | --- |
| N-020 保存検索が `unreadOnly` と `quickFilter='reply'` を保存・復元しない | 保存ビューの型・DB 保存・復元に `unreadOnly` / `quickFilter` を追加。`due` を `quickFilter` へ潰していた復元経路を直し、`reply` が `all` へ落ちないようにした。旧データ（両フィールド無し）はそのまま読める後方互換 | `apps/web/src/app/chats/saved-view-types.ts`、`saved-view-summary.ts`、`page.tsx`、`apps/web/src/components/chats/saved-view-dialog.tsx`、`packages/db/src/saved-searches.ts` |

## Issue #956 — 一斉配信のメッセージ形式（N-063）

| 指摘 | 対応 | 主な変更箇所 |
| --- | --- | --- |
| N-063 メッセージ形式の切り替えがキーボードで動かせない | `role="tablist"`/`tab`/`aria-selected` を保ったまま、←→・Home・End の矢印キー操作とロービング `tabIndex`（選択中だけ Tab で入る）を追加。無効な形式は飛ばし、端で折り返す。クリック動作は不変。キー判定は export した helper で直接検証 | `apps/web/src/components/broadcasts/broadcast-form.tsx`、`broadcast-message-type-keys.test.tsx` |

## Issue #957 — 本人のメールアドレス変更（N-433）

| 指摘 | 対応 | 主な変更箇所 |
| --- | --- | --- |
| N-433 本人のメール変更が即時反映で確認フローが無い | 招待用 `pending_email` とは別に `email_change_new` / `email_change_token_hash` / `email_change_expires_at` を staff に追加（migration 439。公開中 PR #356 が `438_rich_menu_ops.sql` を使うため採番を繰り上げ）。変更要求で即時には差し替えず、新アドレスへ確認メール（`#token=` fragment で秘密をログへ残さない）・旧アドレスへ通知を送信。公開画面 `/staff/email-change` のボタン POST だけが確定し、トークンは一度きりで期限切れあり。OpenAPI と auth 境界テストを更新 | `packages/db/migrations/439_staff_email_change.sql`、`packages/db/src/staff.ts`、`apps/worker/src/services/staff-invite.ts`、`apps/worker/src/routes/staff.ts`、`apps/worker/src/middleware/auth.ts`、`apps/worker/src/routes/openapi.ts`、`apps/web/src/lib/api.ts`、`apps/web/src/lib/auth-email.ts`、`apps/web/src/app/staff/email-change/page.tsx`、`apps/web/src/app/staff/page.tsx` |

## 検証

- `apps/worker`: notification-center / route-guard / auth / chats v6 / staff-email-change / openapi の 6 ファイル 138 件 pass
- `packages/db`: 249 ファイル 1,551 件 pass
- `apps/web`: 変更領域の契約・回帰テスト pass（broadcast キー操作 4 件、saved-view、route-integrity、auth-email、design-debt/design-impact、page-title、dashboard 各契約など）
- `apps/web` 全量: 666 ファイル中 661 pass。失敗 5 ファイル（hq/billing、ops/announcements、ops/support、hq/support/detail、hq/platform-notices、計 20 件）は本ブランチ未変更の領域で、クリーンな base `1d8f31da0` でも同じ失敗を確認済み（環境起因・既存）
- 型検査: `apps/web` pass（`apps/worker`・`packages/db` は先行レーンで pass 済み、今回の追加変更は web のみ）

## 基準値・契約の更新（意図的）

- ページ数契約 181 → 182（`/notifications`、`/staff/email-change` の追加）
- `design-impact-baseline.txt` に Button 利用先 2 行を追加
- 出荷予定・未対応カードの契約テストは禁止文言の旧リテラル期待を `STATE_TEXT` 前提へ更新
- `header-removal` 契約は通知一覧の `usePageTitle('通知')` へ更新
- `/staff/email-change` の関数名を `confirm` → `applyChange`（no-browser-confirm 検査の誤検知回避。ブラウザ confirm は未使用）

## 未検証（要フォロー）

- `/notifications`・`/staff/email-change` は新規画面のため V6 視覚一致証拠（Pencil 実ノード ID・設計画像・実装スクリーンショットの目視比較）は未取得。PR の Visual Parity 欄は `unverified` とする。
- メール送信自体は Worker 側の送信サービスへ委譲。実配信の確認は検証環境での動作確認が必要。
