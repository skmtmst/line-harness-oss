# ここの画像は「前回と同じか」を見るためのものです

**設計に合っている絵ではありません。** 見張っているのは
「知らないうちに変わっていないか」の1点だけです。

**この folder の画像は、どれも既定では「未確認」です。** 設計と合っている、と
言えるのは、突き合わせた文書がそう書いているものだけです。

| 機能 | 突き合わせ | いまの判定 |
|---|---|---|
| 4 友だち属性（V6） | `docs/design-qa/friend-attributes-v6/design-qa.md` | **未一致（要修正）** |

いま `tags-fields` `tags-marks` `tags-searches` の3枚は、設計
`HBTk0` `rIhbN` `QKx8Q` に届いていません。KPI・絞り込み・いくつかの列が
実装にありません。**この絵に合わせて設計を直すのではなく、設計に合わせて
実装を直します。**

実装を直したら、ここの画像を撮り直し、上の文書の判定も一緒に更新して
ください。片方だけ更新すると、直っていないものが「合格」に見えます。

## モックが返すものを増やしたら、撮り直す

`scenarios` の2枚は、**空の一覧のまま1か月放置されていました。** 撮った
時点ではモックが `/api/scenarios` を返しておらず、「シナリオがありません」
が正しい絵でした。あとから固定データが足されても、誰も撮り直さなかったので、
**行が1本も出ない絵を基準に、行の描き方を見張り続けていた**ことになります。

`mock-api.mjs` に口を足したときは、その口を使う画面の基準画像を必ず撮り直して
ください。
