# corrections.md — 修正指示の蓄積(恒久ルール)

> オーナーから受けた「こう直して」「こうして」を **1 件 3 行**で蓄積する。全エージェントは起動時にこれを読み、ここに書かれた指示を恒久的に守る。同じことを二度言わせない。

## 書き方

```
## YYYY-MM-DD
- 指摘: (オーナーが何を直してほしいと言ったか。具体的に)
- 今後: (次から自分がどう動くか。「する / しない」で)
```

## 運用ルール(AI 向け)

- 訂正・要望を言われたら、**その場で**ここに追記する(司令塔がまとめてもよいが、後回しにしない)。
- 抽象的に書かない。「丁寧に」ではなく「見出しは体言止めにする」のように行動で書く。
- 既存と矛盾する新しい指示が来たら、古い行に `(YYYY-MM-DD 更新)` を付けて最新を優先する。
- 月に一度、重複・解決済みを整理する。

---

<!-- ここから下に追記。最新を上に。 -->

## 2026-09-04
- 指摘: 司令塔が他ノードの端末に書いた指示は Enter が届かず入力欄に残っていた(人が送信した)。他ノードへ書くときは、書いた後に必ず改行を別に送って、送信されたことを確認する
- 今後: 他ノードの端末に文を書いたら、文とは別の操作で改行(Enter)を送る。そのあと端末の出力を読み、入力欄に文が残っていないこと・相手が受け取ったことを確かめてから次へ進む。確認できないときは Issue コメントに同じ指示を書く

## 2026-09-04
- 指摘: API 作り直しは #47 の次に #94 を優先し、S3 の停止中 Issue #48・#68・#70・#75・#51・#36 から不足する API・DB を拾う
- 今後: #47 が PR 済みなら #94 を完了し、指定6 Issueのコメントを確認して不足分を `[codex]` チケット化し、自分で着手する

## 2026-09-04
- 指摘: 保護ルール対応で司令塔が PR ブランチへ `codex/development` を取り込むことがある
- 今後: push 直前に必ず `git pull --no-rebase origin <自分のブランチ>` を実行し、rebase と force push は使わない

## 2026-09-04
- 指摘: チケットが終わっても止まらず、PR 後はマージを待たず次のチケットへ進む
- 今後: 自分の lane を blocked → doing → todo の順に `gh` で確認し、1 件を doing にして着手する。依存待ちは対象だけ blocked にして依存先を書き、空き時のみ司令塔に Issue コメントする

## 2026-09-03
- 指摘: lane の作業順は「停止中 → 作業中 → 未着手」とし、品質チェック・エラー修正ノードへ依頼を出さない
- 今後: 自分の lane の `blocked` と `doing` を `todo` より先に処理し、不具合は担当範囲内なら自分で直し、範囲外なら Issue にする

## 2026-09-03
- 指摘: 看板(NodeTerm の Kanban)で PR 番号が分かるようにしてほしい
- 今後: PR を作ったら Issue の題名の末尾に ` (PR #番号)` を足す。番号が付くまでは付けない(先取り禁止)。司令塔は夕方のまとめで、PR 欄に番号があるのに題名に無い Issue を直す

## 2026-09-03
- 指摘: PNG を版から外す作業は撮影側が既にやっているので、司令塔はやらない
- 今後: 進行中の作業と重なる変更は、着手前に「誰がやっているか」を確認し、重なるなら手を出さず情報だけ渡す

## 2026-09-03
- 指摘: 判断は司令塔のおすすめで決めてよい(権限モデル、版の分類、二者承認、Playwright 基準画像、/pools、正本、テナント、主ボタン色、履歴書き換え)。Lステップ検証用アカウントは契約しない
- 今後: 判断が要る点は「おすすめ + 理由」を添えて決め、決定を docs/v6-directives.md §4 と PR コメントに記録する。Lステップの未確認前提は「未確認」のまま残す

## 2026-09-03
- 指摘: 古い要件定義は削除するか見ないようにしておく
- 今後: V2〜V5 世代の文書は docs/archive に置き、冒頭に廃止の目印を付ける。要件として読むのは索引が指す 34 本だけ

## 2026-09-03
- 指摘: UI/UX の採点は実装ではなく Pencil(Pen.dev)の V6 設計そのものを見て付ける
- 今後: 設計の評価は docs/design-reference の設計画像(または Pencil 本体)だけを根拠にし、実装画像と混ぜない

## 2026-09-03
- 指摘: 要件定義を直して(書き直しを含む)
- 今後: 横断契約(アクション一覧、版の 2 分類、権限、再試行、未取得表示、工程ゲート)は索引 §5 の正本に従い、機能別要件に別の流儀を書かない

## 2026-09-02
- 指摘: UI/UX は V6 の Pencil どおりの設計と要件にしたい
- 今後: 画面の正本は Pencil ★V6。設計を変えるときは Pencil を先に直し、コードだけで直さない

## 2026-09-02
- 指摘: 行動は書かなくてよい。調査と提案を書いて
- 今後: 「調査して」「教えて」の依頼では実行に移らず、確認済み事実と推測を分けた評価と提案を返す

## 2026-09-04
- 指摘: 自分のPRはマージせず、ゲートSUCCESS後はIssueへ「ゲート合格」とコメントして次へ進む
- 今後: マージは司令塔の統合PRに任せ、自分はPR作成・ゲート合格コメントまでで完了とする
