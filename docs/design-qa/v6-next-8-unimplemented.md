# 未実装33枚のうち、次に作る8枚

**書いた日**：2026-08-29 ／ **調べた木**：#572 head `e4ab641f`（`codex/development` 直結）

## いちばん大事なこと

**削除確認の5枚は「新しい口が要る」と記録してきましたが、二段に分けられます。**

いまこの5画面は、どれも**ブラウザ標準の `confirm()`** で消しています。

| Node | いまのコード |
|---|---|
| `szXsT` | `rich-menus/page.tsx:193` `confirm('「${group.name}」を削除します。元には戻せません。')` ほか2か所 |
| `yPkWe` | `contents/vars/page.tsx:150` |
| `YfTfJ` | `contents/page.tsx:175`（一括）、`:183`（409のあと「それでも削除しますか？」） |

**つまり削除の口はもう在ります。** 足りないのは窓と、設計が求める「影響確認」です。ここを分けます。

- **段1（いま作れる）** 標準 `confirm` を `ConfirmDialog` へ替え、**何が消え・何が残り・戻せない**の3つを本文に書く。**新しい口は要りません**
- **段2（口が要る）** 使用先の台帳、差し替え、アーカイブ、停止への移行

**段1だけでも、いまの危うさは消えます。** 同じ形を [#553](../../../../pull/553)（`dqFft`）と [#554](../../../../pull/554)（`EGMb1`）が2回やっていて、手本は `Y0Sn3`（リマインダ）と `Gy9OK`（自動応答）です。

**段1を「設計どおり」と扱わないでください。** 影響確認が無いままなので、判定は `needs_fix` のままです。

---

## 8枚の整理

### 1〜5　削除確認（段1はいますぐ、段2は口が要る）

| Node | ルート | 触るファイル | 重なるopen PR |
|---|---|---|---|
| `szXsT` 12-1-F | `/rich-menus` | `app/rich-menus/page.tsx` | #435 #509 **#523** |
| `gBp2J` 13-1-E | `/form-submissions` | `app/form-submissions/page.tsx` | #258 **#436** #457 **#556** |
| `yPkWe` 14-1-C | `/contents/vars` | `app/contents/vars/page.tsx` | #258 **#437** |
| `YfTfJ` 15-1-C | `/contents` | `app/contents/page.tsx` | #258 **#438** **#559** **#560** |
| `UIaM7` 18-1-E | 流入リンク | `app/entry-routes/page.tsx` | **なし** |

**既存部品で作れるか（段1）**：作れます。`components/shared/confirm-dialog.tsx` と、各ページに既にある削除の口。

**新規API・DBが必要か（段2）**：要ります。中身は画面ごとに違います。

- `szXsT` 現在表示中の人数、次に出るメニュー、切替元、参照元。LINE取り下げの実行記録
- `gBp2J` フォーム名・公開状態・回答数・利用中の場所・開けなくなるURL。公開中や回答ありは物理削除せず `status` / `deleted_at` で保管へ
- `yPkWe` 使用先と版、変更前後の文、互換種類の代替候補、全使用先の差し替え、旧定義のアーカイブ
- `YfTfJ` 使用先の完全な台帳、差し替え互換性の事前確認、一括差し替え
- `UIaM7` 投稿・広告・自動処理・成果対応の使用先、過去流入数、転送先候補

**受入条件（段1）**

1. ブラウザ標準の `confirm` を使わない
2. 本文に**何が消え・何が残り・戻せない**の3つを書く
3. 対象の名前を本文に入れる
4. `ConfirmDialog` を使う（新しい部品を作らない）
5. 失敗したときの文を窓の中に出し、窓を閉じない
6. 1440・1920で横スクロール0

**推奨する実装順**：`UIaM7` → `szXsT` → `yPkWe` → `gBp2J` → `YfTfJ`

**`UIaM7` が先**です。重なるopen PRが**1本もありません**。次が `szXsT`（#523 は帯の直しで、削除の行とは別の場所）。**`YfTfJ` は最後**——`contents/page.tsx` に #438 #559 #560 が積み重なっていて、同じファイルの取り合いになります。

### 6　`GMvBd` 4-3-A 対応マークを追加・編集

| | |
|---|---|
| ルート | `/tags?tab=marks` |
| 触るファイル | `app/tags/page.tsx` |
| 重なるopen PR | #264 #420 |
| 既存部品で作れるか | **半分だけ。** 名前・色・並び順・初期値までは作れます |
| 新規API・DBが必要か | **要ります。** 設計の「自動変更ルール」（担当者を割り当てたときなどに、きっかけでマークを自動で変える）に、新しい口とテーブルが要ります |

**受入条件（段1）**：名前・色・並び順・初期値を1枚の画面で編集でき、保存の成否を窓の中に返す。自動変更ルールの欄は**出さない**（作れないものを出さない）。

### 7〜8　`ec9vg` 9-1-F 最終確認 ／ `quhg6` 9-1-G 有効化完了

| | |
|---|---|
| ルート | `/friend-add-settings` |
| 触るファイル | `app/friend-add-settings/page.tsx`、`components/friend-add/`（新規） |
| 重なるopen PR | #258 #420 **#431** **#506** |
| 既存部品で作れるか | **作れません。** 公開版を固定する口が要ります |
| 新規API・DBが必要か | **要ります。** `friend-add-rules` の draft / validate / conflicts / test / publish と冪等キー |

**ただし、手本がもう在ります。** [#551](../../../../pull/551) がリマインダで**まったく同じ契約**を作りました（`s7T2dz` `JCz6J` `W98zZQ` `s6Vvp` `PSmHo` の5枚を確認済み）。

- `POST /api/reminders/drafts` → `GET/PUT /:id/draft` → `POST /:id/preview` → `/validate` → `/test-send` → `/publish`
- 画面は `components/reminders/reminder-publish-flow.tsx` の1枚で、`?stage=` で段を切り替える

**推奨する実装順**：**#551 が取り込まれてから**、その契約と部品をそのまま `friend-add-rules` へ横展開する。先に作ると2つの流儀ができます。

**受入条件**

1. 公開する版を固定し、既に購読している人の版を変えない
2. 公開前チェックの各項目を、通った／注意で言い分ける
3. 数えていないものは `—`（`0` にしない）
4. 完了画面の人数が、公開前チェックの人数と食い違わない
5. テスト送信が本番の登録も予定も動かさず、それを画面に書く

---

## まとめ：着手できる順

| 順 | Node | いま作れるか | 待つもの |
|---|---|---|---|
| 1 | `UIaM7` | 段1 ○ | なし |
| 2 | `szXsT` | 段1 ○ | なし |
| 3 | `yPkWe` | 段1 ○ | #437 |
| 4 | `GMvBd` | 段1 ○（自動変更ルールは出さない） | #420 |
| 5 | `gBp2J` | 段1 ○ | #436 → #556 |
| 6 | `YfTfJ` | 段1 ○ | #438 → #559 → #560 |
| 7 | `ec9vg` | ✗ | **#551 の取り込み** |
| 8 | `quhg6` | ✗ | 同上 |

**段2（影響確認）は6画面ぶんまとめて設計してください。** 使用先を返す口・差し替え・アーカイブは、画面ごとに作ると6通りの流儀ができます。束11（新しいAPI・DBが要る）に置いてあります。
