# 要修正172枚を、横断の束にまとめる

**画面ごとに直すと、同じ間違いを32回直すことになります。**

同じ形の欠けが機能をまたいで並んでいたので、束にしました。
数は `scripts/visual-qa/screens.mjs` の `verdict` と `verdictNote` から
出しています。**手で数えていません。**

> 束に入る画面は重なります（1枚が2つ以上の束に入ります）。
> 合計は172枚になりません。

---

## 先に手本を決める

直す前に、**すでに正しく出ている画面**を決めておきます。
言い回しをそこへ寄せれば、32機能でばらばらになりません。

| 何の手本か | Node | 画面 | どこが良いか |
|---|---|---|---|
| **一覧の状態** | `dC0yg` | 7-1-J リマインダ | 失敗のとき帯は `—`＋「取得できませんでした」、一覧の中は空の文でなく「いまは読み込めていません。上の案内をご覧ください。」。**作成の誘いを出さない** |
| **一覧の状態** | `TmHjF` | 6-1-N 一斉配信 | 同上。帯も本文もそろっている |
| **実行の記録** | `t7UtYQ` | 8-1-K 自動応答の実行結果 | 失敗と「何もしていない」を分ける。見送りを成功と混ぜない |
| **実行の記録** | `DkPY0` | 25-1-C オートメーション実行記録 | 既存の `automation_runs` を読むだけで新しい表を作らない |
| **未取得と0の区別** | `HIU5O` | 17-1-E マイル明細 | 「30日以内に失効 **— mile**（未取得）」。0と書かない |
| **未取得と0の区別** | `z3PB2` | 17-2 行動スコア | 「点数が変わった理由は未取得」と本文で言う |
| **確認のしかた** | `d8d3Mz` | 19-1-C 成果地点の削除確認 | 「そのまま残るもの」（過去の成果128件・金額¥384,000）と「使われている場所」を出す。危ない選択肢を出さない |
| **押せないことを正直に出す** | `w72a2` | 2-12 絞り込み | 繋がっていない条件を `disabled`＋「まだ絞り込めません」。押せて何も起きないより良い |
| **できないと断る** | `cpdDi` | 27-1-B 電話の予約 | 「LINE未連携の電話客は、顧客台帳の受け皿ができるまで登録できません。別の友だちへ推測で結び付けません。」 |

**共通部品はもうあります。** `apps/web/src/components/shared/list-state.tsx`。
使っているのは一部の画面だけです。

---

## 束1 失敗を空として出す

**いちばん多く、いちばん危ない束です。** 読み込みに失敗したのに
「まだありません」と出ると、持っているものが消えたように見えます。
そのうえ「作成」を誘うので、押せば同じものをもう1つ作ります。

| Node | 機能 | ルート | 重さ | いま出るもの |
|---|---|---|---|---|
| `ZOPyc` | 13 回答フォーム | `/form-submissions` | **P0** | **赤い帯すら出ない。** 空とまったく同じ絵 |
| `h8pBZr` | 15 登録メディア | `/contents` | P1 | 赤い帯＋「ファイルがまだありません。」 |
| `Vdbv5` | 25 オートメーション | `/automations` | P1 | 赤い帯＋「ありません。「新規ルール」から作成してください。」 |
| `f8SBSh` | 26 外部連携 | `/webhooks` | P1 | 赤い帯＋「受信Webhookがありません。「新規Webhook」から作成してください。」 |
| `W6465r` | 28 予約設定 | `/booking/menus` | P1 | 赤い帯＋「まだメニューがありません。上の「メニューを追加」から登録してください。」 |
| `k5m5Bc` | 29 イベント予約 | `/events` | P1 | 赤い帯＋「イベントがまだありません／最初のイベントを作成」 |
| `q5G45` | 5 シナリオ配信 | `/scenarios` | P1 | 赤い帯＋「シナリオがありません。「＋ シナリオを作成」から作ってください。」 |
| `bzDn6` | 3 友だち | `/friends` | P1 | 赤い帯＋「条件に合う友だちが見つかりません」＋「0件」 |

**直し方**：`list-state.tsx` を当てて、`error` の枝を足す。
**失敗のときは `action`（作成の誘い）を渡さない。**

**既存部品で直せます。新しいAPIは要りません。**
`yKEdO`（4-2-C 友だち情報欄）は **#420 で同じ直しが入って解決しています。**
そのPRの差分がそのまま手本になります。

---

## 束2 `API error: 500` などの内部文言が画面に出る

受け取った文字をそのまま描いています。`ApiError` の既定文が
`API error: <status>` なので、**英語と数字が運用の人に出ます。**

| Node | 機能 | ルート | 重さ | 出ている文字 |
|---|---|---|---|---|
| `RW5Tb` | 12 リッチメニュー | `/rich-menus` | P1 | `API error: 500`（`rich-menus/page.tsx:452` が `{error}` を直に描く） |
| `zCQXe` | 10 ウェビナー | `/webinars` | P1 | `API error: 500`（赤枠の説明文として） |
| `W6465r` | 28 予約設定 | `/booking/menus` | P1 | `API error: 500` |
| `k5m5Bc` | 29 イベント予約 | `/events` | P1 | `API error: 500` |

**直し方**：受け取った文字をそのまま出さず、状態から日本語を選ぶ。
`ApiError.status` は持っているので、403／404／409／500 で言い分けられます。
`s6MBc`（17-2-A 行動スコアのルール）で同じ形を入れてあります
（`apps/web/src/app/mileage/score-rules/page.tsx` の `fieldError`）。

---

## 束3 作り手の言葉が画面に出る

| Node | 機能 | 出ている言葉 | 重さ |
|---|---|---|---|
| `xfYLn` | 5 シナリオ | 「**cron** が 5 分粒度のため最大 5 分遅れる場合があります」 | P1 |
| `q8wSqO` | 8 自動応答 | 「**silent rule** のみ — **match** するが返信しない (同 **keyword** の **automation rule** 未登録)」「適用外 (**line_account_id** が別アカに固定)」「返信あり (**inline**)」、列見出しの **TEMPLATE** | P1 |
| `r7eSi` | 3 統合ユーザー | 識別子の列に **`identity-0`** | P1 |
| `u6gHt` | 6 一斉配信の結果 | 「1通（**carousel**）」 | P2 |
| `p97Tf` | 6 テンプレート選択 | カードの補足に **`text`** | P2 |
| `NKyoA` | 11 テンプレート | 区分のチップに **Flex** | P2 |
| `f8SBSh` | 26 外部連携 | タブが「受信 (**Incoming**)」「送信 (**Outgoing**)」 | P2 |
| `W6465r` | 28 予約設定 | 「**旧デザインでは**「メニュー」と「スタッフ」が別ページに分かれていました。」（作り替えの覚え書き） | P2 |

**直し方**：言葉の置き換えだけ。**新しいAPIは要りません。**
`carousel` → カルーセル、`text` → テキスト のような対応表を1か所に置くと、
テンプレート・一斉配信・シナリオで使い回せます。

---

## 束4 未取得を `0件` として出す

**「まだ数えていない」と「数えたら0だった」は違います。**
0と書くと「誰もいない」と読まれます。

| Node | 機能 | どこが | 重さ |
|---|---|---|---|
| `q8wSqO` | 8 自動応答 | 失敗のとき帯が 0件・0回・0件・0件 | P1 |
| `NKyoA` | 11 テンプレート | 失敗のときタブの件数とフォルダ件数が 0 | P1 |
| `zCQXe` | 10 ウェビナー | 失敗のときウェビナーだけ 0件（ほか3枚は `—`） | P1 |
| `Vdbv5` | 25 オートメーション | 失敗のときルールが 0件 | P1 |
| `W6465r` | 28 予約設定 | 失敗のとき 0件・0人・0件 | P1 |
| `k5m5Bc` | 29 イベント予約 | 失敗のとき 0件・0人・0件 | P1 |
| `bzDn6` | 3 友だち | 失敗のとき「友だち一覧 0件」 | P1 |
| `HBTk0` | 4 友だち情報欄 | 「入力済み」が未取得なのに `0人`（`?withUsage=1` を付けずに読んでいる） | P1 |
| `q76C35` | 6 一斉配信 | 欠けた項目をそのまま文へ繋ぎ `undefined` と出る（`broadcast-kpis.tsx:40`） | P2 |
| `u6gHt` | 6 一斉配信の結果 | 同じく `1通（undefined）`／`Invalid Date 作成` | P2 |
| `bV5Vs` | 5 シナリオ編集 | 到達率が取れないと `NaN%`（`scenario-detail-client.tsx:1510`） | P1 |

**直し方**：`?? null` で受けて `—` を描く。文字列に繋ぐ前に有無を見る。
`HIU5O` の「— mile（未取得）」が手本です。

> `q76C35` `u6gHt` `bV5Vs` の `undefined` / `Invalid Date` / `NaN%` は、
> **画面確認の固定データが足りなかったこちらの落ちでもあります**（直しました）。
> ただし実装側に守りが無いのは残ります。本番の口が欠けた返事をすれば同じ絵になります。

---

## 束5 取り消せない操作の確認が足りない

| Node | 機能 | いまの形 | 重さ |
|---|---|---|---|
| `dqFft` | 5 シナリオの通を削除 | ブラウザ標準の `confirm('このステップを削除してもよいですか？')`。どの通か、配信対象と送信後アクションも消えること、到達済みの履歴は残ることを言わない | P1 |
| `EGMb1` | 6 配信を削除 | 標準の `confirm('この配信を削除してもよいですか？')`。どの配信か、予約中か送信済みかを言わない | P1 |
| `zGZMA` | 4 対応マークを削除 | 何人に付いているか、どのマークへ移るかを言わない（人数はそもそもどの口も返していない） | P1 |
| `h0kahp` | 6 テスト送信 | 押すと**配信を1件作ってから送る**。失敗しても作った配信は残る（`broadcast-form.tsx:514`）。送信先も選べない | P1 |
| `M9cij` | 11 テンプレートを削除 | 使用中は削除の導線ごと外しており危険は無いが、設計の「先に差し替えてから削除する」流れが無い | P2 |
| `p97Tf` | 6 テンプレート選択 | 読み込む前の確認窓が無い | P2 |

**直し方**：`ConfirmDialog` は既にあります。標準の `confirm` をやめ、
**何が消えて何が残るかを本文に書く。** `d8d3Mz` が手本です。
`zGZMA` だけは人数のAPIが要ります（束8を見てください）。

---

## 束6 出ている数どうしが合わない

| Node | 機能 | 何が合わないか | 重さ |
|---|---|---|---|
| `TC1b1` | 5 シナリオ配信 | 購読中1,028人・読了済728人と出しながら**完了率41%**。読む人が画面から計算すると71%（`scenarios/page.tsx:234` が `completed/(subscribers+completed)`） | P1 |
| `u6gHt` | 6 一斉配信の結果 | 送信624件・到達624件と並べて**開封2,410件**。手元の記録とLINE側の集計を混ぜているのに、どちらの母数かを言わない | P1 |

**直し方**：どちらも計算そのものより、**母数を画面に書く**ことで解けます。
「読了済 728人（購読中1,028人のうち71%）」のように。

---

## 束7 設計の面が丸ごと無い

**部品を当てて直る話ではありません。** 作る量の見積りが要ります。

| Node | 機能 | 何が無いか | 重さ |
|---|---|---|---|
| `Igi72` | 3 詳細検索 | OR条件が4つだけで全部押せない。「表示する友だち」が丸ごと無い | P1 |
| `YzxU1` | 3 重複検出 | 重複候補の一覧が無い（設計の中心）。アカウント間マトリックスも無い | P1 |
| `I6UAdr` | 3 友だち詳細 | タブの構成も左のカードも設計と別物 | P1 |
| `EvVO5` | 5 開始条件 | 設計の窓が無く、設定欄のセレクト1つ | P1 |
| `hz9ti` | 5 送信後アクション | 選べる動作が8つのうち5つ。順番も条件分岐も無い | P1 |
| `HIU5O` | 17 マイル明細 | ランクの進み・期限の警告・つながる先が丸ごと無い | P1 |
| `MvZm5` | 17 マイルの履歴 | 「だれが」の列が無く、手で動かした記録を誰がやったか追えない | P1 |
| `jwVlo` | 30 入った記録 | 記録の一覧ではなく、ユーザー一覧に「最後の操作」を足したもの（`staff/page.tsx:114`） | P1 |
| `cpdDi` | 27 電話の予約 | 電話番号もお名前も入れられない | P1 |
| `zZ9fA` | 6 一斉配信を作成 | 節の番号が画面の並びと合わない（1→3→2）。5段の進み表示が無い | P1 |
| `vW4Es` | 6 配信前チェック | 送信枠を見ない。足りなくても進める | P1 |
| `Bw0zt` | 6 送信設定 | 送信枠・配信スケジュールの重なり確認・重複時の送信・配信優先度が無い | P1 |
| `XQfMD` | 6 メッセージ編集 | 本文の上限が0/500（設計は1通5,000・合計22,500・最大5通）。ボタン編集とURLの扱いが無い | P1 |

---

## 束8 新しいAPI・DBが要る

**画面だけでは直りません。**

| 要るもの | どの画面 | 取得元の案 | 未取得のとき |
|---|---|---|---|
| 対応マークごとの人数 | `rIhbN` `zGZMA` | `GET /api/support-marks` に `friendCount` を足す（`friends.support_mark_id` を数える） | `—` |
| 情報欄の入力済み人数 | `HBTk0` | `GET /api/friend-fields?withUsage=1`（**型にはもうある**） | `—` |
| 情報欄の回答フォーム・表示先 | `HBTk0` | フォームの `layout` を走査、または `GET /api/friend-fields/:id/usage` | `—` |
| 保存した検索の使用先・該当人数 | `QKx8Q` | `GET /api/saved-searches?withUsage=1` | `—` |
| 各タブの帯 | `HBTk0` `rIhbN` `QKx8Q` | `GET /api/list-stats` に `fields` `marks` `searches` を足す | `—` |
| 一括操作（複数人へタグ・シナリオ・担当者ほか） | `IAf7j` | 新設。画面も別ページとして作る | — |
| 送信枠（残り通数） | `vW4Es` `Bw0zt` | LINE側の月間上限と使用予定 | `—` |
| 操作の記録（何を変えたか） | `jwVlo` | いまは login/logout/fail/view_personal/export の5種類しか記録していない。テンプレート削除・マイル手動増減・配信実行などを足す | — |
| 顧客台帳（LINE未連携の電話客） | `cpdDi` | 新設 | — |

**`ec_events` と `messages_log(source='ec_transactional')` の話**は
[残り4画面の準備](v6-execution-records-remaining-4.md) を見てください。
所属アカウントを確定できない過去イベントは、推測で表示しません。

---

## 束9 内部の言葉（追加ぶん）

束2・束3を書いたあとに注記を深めた結果、**同じ束に入るものが増えました。**

| Node | 機能 | 出ている言葉 |
|---|---|---|
| `GC4St` | 7 リマインダ実行結果 | 内部IDが画面に出ている |
| `DEX0k` | 21 NENコラム | 状態が `scheduled` `sent` `draft` のまま（`page.tsx:422`）。#446 が配信ジョブで直した `jobStatusLabel` をそのまま置ける |
| `HpKyF` | 21 NEN配信の編集 | きっかけが `ec.order.delivered` `pet.birthday` のまま（`edit/campaign-editor.tsx:298-301`） |
| `QKx8Q` `XBkiQ` | 4 保存した検索 | 友だち情報の項目名がキーのまま。対応マークとシナリオがIDの手入力 |

**合計15枚**（束2・束3とあわせて）。どれも言葉の置き換えだけで、
**画面の作りを変えません。**

---

## 束10 取り返しがつかない・あとから追えない（9枚）

**「壊れたことに気づく場所が無い」**という形の欠けです。
どれも、間違いが起きたその瞬間ではなく、**あとで別の形で表に出ます。**

| Node | 機能 | 気づけないこと |
|---|---|---|
| `v9tYhl` | 13 集まった回答 | 情報欄への書き込みが失敗した件数が出ない。**答えは受け取れているのに友だち情報へ入っていない**。あとでリマインダが動かない形で出る |
| `voJtX` | 15 メディア詳細 | 使用中でも消せる。**消したあとに何が壊れたかを知る場所が無い** |
| `gBtaK` | 14 共通情報の編集 | どこが変わるか見えないまま保存する。「会社名」を直すと何本のテンプレートの文が変わるか知らないまま |
| `BuVDB` | 18 広告とのつなぎ | 対応が付いていない成果地点は広告へ返せないのに、**返せていないことに気づく場所が無い** |
| `eI3gs` | 23 EC連携 | 結びつかなかった注文が出てこない。**買ってくれた事実がLINE側に何も残らない** |
| `k3WxrO` | 26 外部連携 | 失敗した記録もやり直す操作も無い。**Slackへの通知が6回落ちていたことに気づけない** |
| `UhC2O` | 32 更新履歴 | 緊急操作の履歴が `localStorage`。**端末を変えると読めず、消せてしまう** |
| `e3jz3` | 30 ログインユーザー | `lastLoginAt` が無い。**辞めた人のログインが生きたまま残る** |
| `jwVlo` | 30 入った記録 | 記録は残っているのに、読む場所が無い |

**この束は、直す量は小さいのに見落とすと重いものが並びます。**

---

## 束ねられない111枚

**残りは「その画面だけの差」です。** 文言・見出し・置き場所・段の分けかたが
設計とずれているもので、**まとめて直す形になりません。**

台帳（`screens.mjs` の `verdictNote`）に1枚ずつ何が違うかを書いてあります。
**「未一致」だけだった94枚は、すべて中身を入れ直しました（残り0）。**

機能ごとの数は [進捗台帳](v6-screen-ledger.md) を見てください。

---

## Codexへ渡す順（上から5本）

1. **束1 失敗を空として出す**（8枚）
   `list-state.tsx` を当てるだけ。**新しいAPIは要りません。**
   `ZOPyc` は P0。#420 の `yKEdO` の差分がそのまま手本になります。

2. **束2＋束3 内部の言葉**（12枚）
   言葉の置き換えだけ。**画面の作りを変えません。**
   `API error: 500` の4枚を先に。

3. **束4 未取得を0件として出す**（11枚）
   `?? null` で受けて `—` を描く。`undefined` / `Invalid Date` / `NaN%` の
   3枚は、文字列に繋ぐ前に有無を見るだけで消えます。

4. **束5 確認が足りない**（6枚）
   標準の `confirm` を `ConfirmDialog` に置き換え、
   **何が消えて何が残るかを書く。** `h0kahp` の「押すたびに配信が1件残る」は
   先に止めたほうがよいので、この束の中では最優先です。

5. **束6 合わない数**（2枚）
   母数を画面に書く。行数は少ないのに、読み違いがいちばん重い。

**束7・束8は見積りが要ります。** 上の5本が片づくまで着手しないほうが、
同じファイルの取り合いになりません。
