# 要修正181枚を、横断欠陥10束にまとめる

**画面ごとに直すと、同じ間違いを32回直すことになります。**

束の中身は `scripts/visual-qa/screens.mjs` の `verdictNote` から機械で
拾い、そのうえで手で見直しています。**数は手で数えていません。**

> 1枚が2つ以上の束に入ります。合計は181枚になりません。
> 判定の最新は [進捗台帳](v6-screen-ledger.md)。2026-08-29 時点で
> **一致 7 ／ 構造一致・データ未接続 16 ／ 要修正 181 ／ 未実装 55**。

---

## 手本にする画面

直す前に、**すでに正しく出ている画面**を決めておきます。
言い回しをそこへ寄せれば、32機能でばらばらになりません。

| 何の手本か | Node | 画面 | どこが良いか |
|---|---|---|---|
| **一覧の状態** | `dC0yg` | 7-1-J リマインダ | 失敗のとき帯は `—`＋「取得できませんでした」、中は「いまは読み込めていません。上の案内をご覧ください。」。**作成の誘いを出さない** |
| **一覧の状態** | `TmHjF` | 6-1-N 一斉配信 | 同上 |
| **一覧の状態** | `ZOPyc` | 13-1-F 回答フォーム | #436 で直った形。⚠と「回答フォームを読み込めませんでした」＋再読み込み。`data-list-state` が3つに分かれる |
| **取り消せない操作の確認** | `Y0Sn3` | 7-1-I リマインダ削除 | 「削除すると未送信の通知予定はすべて取り消されます。送信済みの履歴は監査記録として残り、この操作は取り消せません。」＝**何が消え・何が残り・戻せない**の3点 |
| **取り消せない操作の確認** | `Gy9OK` | 8-1-I 自動応答削除 | 同じ3点セット |
| **取り消せない操作の確認** | `d8d3Mz` | 19-1-C 成果地点 | 「そのまま残るもの」と「使われている場所」を出し、危ない選択肢を出さない |
| **未取得と0の区別** | `HIU5O` | 17-1-E マイル明細 | 「30日以内に失効 **— mile**（未取得）」 |
| **未取得と0の区別** | `P2J0Te` | 9-1-H 実行結果 | 「**取得できない記録は0件にせず「経路は取得できません」と表示します**」と画面に書く |
| **押せないことを正直に出す** | `w72a2` | 2-12 絞り込み | 繋がっていない条件を `disabled`＋「まだ絞り込めません」 |
| **出せないものを出さない** | `TimXl` | 10-1-H 公開完了 | 「LIFF IDを確認できないため、公開ページのボタンは出していません。」 |
| **変える前に影響を見せる** | `uNBlA` | 14-1-B 共通情報 | #548 で入った形。**所属を確定できないものを黙って落とさない**：「所属するLINEアカウントを確認できない回答フォームが3件あります。内容を見せず、安全のため影響件数に含めています。」。取れない上限は `—`＋要確認＋理由 |
| **つながりの検査** | `DIUbO` | 12-1-C 切替のつながり | #509 で入った形。「入口へ戻れません」を実際に見つける。**「番号や内部IDは表示しません」と画面で宣言** |
| **実行の記録** | `t7UtYQ` `DkPY0` | 8-1-H / 25-1-B | 失敗と「何もしていない」を分ける。既存の台帳を読むだけで新しい表を作らない |

**共通部品**：`apps/web/src/components/shared/list-state.tsx`、
`components/shared/confirm-dialog.tsx`。どちらも既にあります。

---

## 束1 失敗を空として出す

**読み込みに失敗したのに「まだありません」と出る。** 持っているものが
消えたように見え、そのうえ作成を誘うので、押せば同じものをもう1つ作ります。

| Node | 機能 / ルート | 重さ | 対象PR | 状態 |
|---|---|---|---|---|
| `ZOPyc` | 13 `/form-submissions` | P0 | **#436** `950073ab` | **解決を確認済み**（2026-08-29） |
| `bzDn6` | 3 `/friends` | P1 | #520 `4848a8f3` | 差分は入っている。画像未確認 |
| `q5G45` | 5 `/scenarios` | P1 | #519 `a8e00234` | 同上 |
| `Vdbv5` | 25 `/automations` | P1 | #516 `a8c9d2a7` | 同上 |
| `f8SBSh` | 26 `/webhooks` | P1 | #515 `09054b78` | 同上 |
| `W6465r` | 28 `/booking/menus` | P1 | #532 `6cc74968` | 同上 |
| `k5m5Bc` | 29 `/events` | P1 | #533 `d1070487` | 同上 |
| `h8pBZr` | 15 `/contents` | P1 | #438 `166f0c43` | 同上 |

**完了条件**（1枚ずつ）
1. `data-list-state` が `loading` / `empty` / `error` の3つに分かれる
2. 失敗のとき、一覧の中に**空の文を出さない**
3. 失敗のとき、**作成の誘い（`action`）を渡さない**
4. 失敗のとき、帯の数を `—` にする（束4と重なる）
5. 1440・1920 の両方で横スクロール0

**手本**：`ZOPyc`（#436 の差分がそのまま使えます）

---

## 束2 内部エラーが生で出る

`ApiError` の既定文が `API error: <status>` なので、**英語と数字が
運用の人に出ます。**

| Node | 機能 / ルート | 重さ | 対象PR | 状態 |
|---|---|---|---|---|
| `RW5Tb` | 12 `/rich-menus` | P1 | #523 `1a943082` | 差分は入っている。画像未確認 |
| `zCQXe` | 10 `/webinars` | P1 | #524 `a6c35ee0` | 同上 |
| `W6465r` | 28 `/booking/menus` | P1 | #532 `6cc74968` | 同上 |
| `k5m5Bc` | 29 `/events` | P1 | #533 `d1070487` | 同上 |

**完了条件**
1. 画面に `API error` の文字列が1つも出ない
2. `ApiError.status` から日本語を選ぶ（403／404／409／500 で言い分ける）
3. 手本は `s6MBc` の `fieldError`（`apps/web/src/app/mileage/score-rules/page.tsx`）

---

## 束3 作り手の言葉が画面に出る

| Node | 機能 | 出ている言葉 | 対象PR |
|---|---|---|---|
| `xfYLn` | 5 | 「**cron** が 5 分粒度のため…」 | #530 `2568c474` |
| `cmDfJ` `q8wSqO` | 8 | `silent rule` `match` `keyword` `automation rule` `line_account_id` `(inline)` `TEMPLATE` | #540 `857cebc5`（実行結果側）。**一覧の青い箱はまだ未対応** |
| `r7eSi` | 3 | 識別子の列に `identity-0` | **無し** |
| `u6gHt` | 6 | 「1通（**carousel**）」 | #531 `b296cb04` |
| `p97Tf` | 6 | カードの補足に `text` | **無し** |
| `NKyoA` | 11 | 区分チップに `Flex` | #528 `1b95452d` |
| `k3WxrO` `f8SBSh` | 26 | 「受信 (**Incoming**)」「送信 (**Outgoing**)」 | #527 `c6fd4388` |
| `DEX0k` | 21 | 状態が `scheduled` `sent` `draft` | #525 `deff5ffb` |
| `HpKyF` | 21 | きっかけが `ec.order.delivered` `pet.birthday` | #526 `dfcc9a53` |
| `QKx8Q` `XBkiQ` | 4 | 項目名がキーのまま／対応マークとシナリオがIDの手入力 | #539 `8f7612de`・#541 `e929f22a` |
| `GC4St` | 7 | 内部IDが画面に出る | #511 `4bc71249` |
| `W6465r` | 28 | 「旧デザインでは…」の作り替えメモ | #532 `6cc74968` |

**PRが無いのは `r7eSi` と `p97Tf` の2枚**です。

**完了条件**
1. 画面に英字の内部値（`text` `carousel` `inline` `scheduled` `identity-` `line_account_id` ほか）が出ない
2. 対応表を1か所に置き、テンプレート・一斉配信・シナリオで使い回す
3. **画面の作りは変えない**（言葉の置き換えだけ）

---

## 束4 未取得を `0件` として出す

**「まだ数えていない」と「数えたら0だった」は違います。**

| Node | 機能 | どこが | 対象PR |
|---|---|---|---|
| `ZOPyc` | 13 | 失敗時も帯が「フォーム 0件」「公開中 0件」 | **#556** `6037aeef`（`form-kpi-value.tsx` を足して未取得と0件を分ける） |
| `q8wSqO` | 8 | 失敗時に帯が 0件・0回・0件・0件 | **無し** |
| `NKyoA` | 11 | 失敗時にタブとフォルダの件数が 0 | #528 `1b95452d` |
| `zCQXe` | 10 | ウェビナーだけ 0件（ほか3枚は `—`） | #524 `a6c35ee0` |
| `RW5Tb` | 12 | メニューと出し分けが 0件 | #523 `1a943082` |
| `Vdbv5` | 25 | ルールが 0件 | #516 `a8c9d2a7` |
| `W6465r` | 28 | 0件・0人・0件 | #532 `6cc74968` |
| `k5m5Bc` | 29 | 0件・0人・0件 | #533 `d1070487` |
| `bzDn6` | 3 | 「友だち一覧 0件」 | #520 `4848a8f3` |
| `HBTk0` | 4 | 「入力済み」が未取得なのに `0人` | **無し**（`?withUsage=1` を付けて読む必要あり） |
| `q76C35` | 6 | 欠けた項目をそのまま繋いで `undefined` | **#557** `697cee2c`（`broadcast-kpi-values.tsx`） |
| `bV5Vs` | 5 | 到達率が取れないと `NaN%` | #534 `0158ba8e` |

**完了条件**
1. 未取得は `—`、実値0は `0件` と描き分ける
2. 文字列に繋ぐ前に有無を見る（`undefined` / `Invalid Date` / `NaN` を出さない）
3. **撮影ハーネスが自動で見張る。** `capture-screens.mjs` の `BROKEN_VALUES`
   が画面にその3つを見つけたら撮らない

---

## 束5 取り消せない操作の確認

| Node | 機能 | いまの形 | 対象PR |
|---|---|---|---|
| `dqFft` | 5 通の削除 | ブラウザ標準の `confirm` | **#553** `2fdded68`（`scenario-detail-client.tsx`） |
| `EGMb1` | 6 配信の削除 | ブラウザ標準の `confirm` | **#554** `875a9ed3`（`broadcasts/page.tsx`） |
| `zGZMA` | 4 対応マークの削除 | 人数も移り先も言わない | **無し**（人数のAPIが要る＝束11） |
| `h0kahp` | 6 テスト送信 | **押すたびに配信が残る件は #543 `819895dd` で解決済み。** 送信先を選べない差は残る | #543 |
| `Y0Sn3` | 7 | **#498→#514 で解決済み。手本** | 済 |
| `Gy9OK` | 8 | **#491→#544 で解決済み。手本** | 済 |

**完了条件**
1. ブラウザ標準の `confirm` を使わない
2. 本文に**何が消え・何が残り・戻せない**の3つを書く
3. 対象の名前を本文に入れる
4. `ConfirmDialog` を使う（新しい部品を作らない）

---

## 束6 出ている数が合わない

| Node | 機能 | 何が合わないか | 対象PR |
|---|---|---|---|
| `TC1b1` | 5 | 購読中1,028・読了済728と出しながら**完了率41%**（読む人の計算は71%） | #529 `4cb2b0d9` |
| `u6gHt` | 6 | 送信624と並べて**開封2,410**。どちらの母数か言わない | #531 `b296cb04` |
| `xqT1Z` `jwrbf` | 16 | 報酬が「売上 × 率」でしか出ず、決まった額の人が**ずっと ¥0** | **無し** |

**完了条件**：母数を画面に書く。「読了済 728人（購読中1,028人のうち71%）」の形。

---

## 束7 設計の面が丸ごと無い（23枚）

**部品を当てて直る話ではありません。作る量の見積りが要ります。**

機能5 `EvVO5` `RUxNf` ／ 機能3 `Igi72` `YzxU1` ／ 機能7 `uJP22` ／
機能8 `K7vg2` `nzWIX` `ivDoe` ／ 機能10 `PV1Vh` `Xjk8q` `yxyzQ` ／
機能11 `hsBtl` `J3GxEZ` `M9cij` ／ 機能12 `kQ1bs` `UMiJ9` ／ 機能15 `eXAJP` ／
機能16 `PouPn` ／ 機能17 `HIU5O` ／ 機能26 `k3WxrO` ／ 機能4 `sfTEW` `HBTk0`

**多くは「設計の A・B・C が実装では同じ1枚」という形**です
（`K7vg2`/`nzWIX`/`ivDoe`、`PV1Vh`/`Xjk8q`、`kQ1bs`/`UMiJ9`）。
**画面を割るか、設計を1枚に寄せるかの判断**が先に要ります。

**完了条件**：束7は**先に商品仕様の判断**。それまで着手しない。

---

## 束8 1440pxで切れる（2枚）

`GC4St`（7-1-H 実行結果）／ `ZrpKn`（19-1 成果地点名が `…` で切れる。設計は折り返す）

**完了条件**：1440で列も文字も切れない。撮影ハーネスの「はみ出し=0」だけでは
足りない（**中の列が切れても0のまま**）ので、画像で見る。

---

## 束9 取り返しがつかない・あとから追えない（10枚）

**「壊れたことに気づく場所が無い」**形。**直す量は小さいのに、見落とすと重い。**

| Node | 機能 | 気づけないこと | 対象PR |
|---|---|---|---|
| `v9tYhl` | 13 | 情報欄への書き込みが失敗した件数が出ない | **無し** |
| `voJtX` | 15 | 使用中でも消せる。消したあと何が壊れたか分からない | **無し** |
| `gBtaK` | 14 | どこが変わるか見えないまま保存する | **#548 `d4a85ad4` で解決を確認済み**（保存を押すと影響の面へ進む） |
| `BuVDB` | 18 | 対応が付いていない成果地点に気づけない | **無し** |
| `eI3gs` | 23 | 結びつかなかった注文が出てこない | **無し** |
| `k3WxrO` | 26 | 失敗した記録もやり直す操作も無い | #547 `48715569` |
| `UhC2O` | 32 | 緊急操作の履歴が `localStorage` | **無し** |
| `e3jz3` | 30 | `lastLoginAt` が無い | **無し** |
| `jwVlo` | 30 | 記録は残っているのに読む場所が無い | **無し** |
| `festr` | 24 | 送った記録が残らない | #504 `a7aeda33`・#545 `03022681` |

**完了条件**：失敗・変更・実行が**あとから1画面で辿れる**こと。
数を作らず、記録が無ければ `—` と言うこと。

---

## 束10 押せない・導線が無い（17枚）

**押せる形で置いてあるのに何も起きない**、または**必要な導線が無い**もの。

| Node | 何が押せない・無い | 対象PR |
|---|---|---|
| `bPF0s` | **予約を取り消す**導線が無い | **無し** |
| `GB0NR` | 公開ページを見るが押せない（LIFF ID未確認。理由付きで止めるのは正しい） | #507→#508 |
| `NWbuF` | フォルダ一覧に「未分類」が出ない | #536 `54cddd5e` |
| `w72a2` | 期限とメッセージ種別が押せない（正直な出し方。口が要る） | **無し** |
| `AuSDY` | 未入力でも保存ボタンが押せる（設計は無効） | **無し** |
| `cCB7r` | フォルダをこの画面で選べない | **無し** |
| `Igi72` | OR条件が4つで全部押せない | **無し** |
| `r7eSi` | 統合ユーザーを作る導線が無い | **無し** |
| `ZC13r` `VLMGH` `gief7` | 「並び替え」「フォルダを追加」が理由なしに押せない | #525（NEN配信のみ） |
| `MvZm5` | 手で増やす・減らす／CSVの導線が無い | **無し** |
| `d8d3Mz` | 使っている場所を開く導線が無い | **無し** |
| `TV2DI` `cpdDi` | 「予約を追加」が押せない | #459 `ba0bf62d` |
| `h0kahp` | 送信先を選べない | **無し** |
| `Ho8z4` | テスト送信・公開ページを見るが押せない | **無し** |

**完了条件**
1. 押せるのに何も起きないものを残さない
2. 使えないなら**出さない**。出すなら**理由を添えて `disabled`**（手本は `w72a2` と `TimXl`）

---

## 束11 新しいAPI・DBが要る

**画面だけでは直りません。** [残り4画面の準備](v6-execution-records-remaining-4.md)
と合わせて読んでください。

| 要るもの | 画面 | 取得元の案 | 未取得のとき |
|---|---|---|---|
| 対応マークごとの人数 | `rIhbN` `zGZMA` | `GET /api/support-marks` に `friendCount` | `—` |
| 情報欄の入力済み人数 | `HBTk0` | `GET /api/friend-fields?withUsage=1`（**型にはある**） | `—` |
| 情報欄の回答フォーム・表示先 | `HBTk0` | フォームの `layout` を走査 | `—` |
| 保存した検索の4指標 | `QKx8Q` | #541 で実装中 | `—` |
| 一斉配信の保存条件 | `cPk8A` `sqFXf` | #542 で実装中 | — |
| 送信枠（残り通数） | `vW4Es` `Bw0zt` | LINE側の月間上限と使用予定 | `—` |
| 操作の記録（何を変えたか） | `jwVlo` | いまは5種類しか記録していない | — |
| 顧客台帳（LINE未連携の電話客） | `cpdDi` | 新設 | — |
| マイルの使い道 | `qlVLJ` `p9CcEB` | **Codexが実装中** | — |

---

## 束に入らない109枚

**その画面だけの差**です。文言・見出し・置き場所・段の分けかたが
設計とずれているもので、まとめて直す形になりません。
`screens.mjs` の `verdictNote` に1枚ずつ書いてあります。

---

## #550〜#559 が、どの束のどれに当たるか

**PRの名前ではなく、変えたファイルと画面の Node で対応づけました。**
名前は書き手の言葉なので、同じ言葉でも別の画面を触っていることがあります。

| PR | head | 変えたファイル（要点） | Node | 束 |
|---|---|---|---|---|
| #550 | `f7c5a99e` | `components/broadcasts/segment-preset*` `broadcast-form.tsx` | `cPk8A` `sqFXf` | 束7（設計の面が無い） |
| #551 | `44692a37` | `components/reminders/reminder-publish-flow.tsx` `app/reminders/new`・`edit` | `s7T2dz` `JCz6J` `W98zZQ` `s6Vvp` `PSmHo` | 束7 |
| #552 | `6ce43563` | `components/automations/automation-template-gallery.tsx` `automation-draft-editor.tsx` `app/automations/page.tsx`（タブ帯5本と権限） `new/page.tsx` | `WjYAC`（印あり）＋ `gief7` `Rv8Jv` `Vdbv5`（同じページを描き替えるため） | 束10（導線が無い） |
| #553 | `2fdded68` | `app/scenarios/detail/scenario-detail-client.tsx` | `dqFft` | **束5** |
| #554 | `875a9ed3` | `app/broadcasts/page.tsx` | `EGMb1` | **束5** |
| #555 | `e873eeb9` | `components/chats/saved-view-dialog.tsx` | **`AuSDY`**（未入力エラーはこの画面）＋ `ANgda` `tBlkL` `LHjwD`（同じ部品） | P0-1 の続き |
| #556 | `6037aeef` | `app/form-submissions/form-kpi-value.tsx` `page.tsx` | `ZOPyc` | **束4** |
| #557 | `697cee2c` | `components/broadcasts/broadcast-kpi-values.tsx` | `q76C35` | **束4** |
| #558 | `ef7b5773` | `app/affiliates/affiliate-reward.ts` `tabs.tsx` | `PouPn` `xqT1Z` `jwrbf` | 束6（数が合わない）— **画像確認済み。4項目とも通った** |
| #559 | `888e80f9` | `app/contents/page.tsx` `components/shared/filter-chip.tsx` | `g89Tc` | **束4** — **画像確認済み。7項目のうち6つ通り、P1が1つ残る** |

**Node の決め方。** まず差分の `data-design-node` を数え、印が無いPRは
**変えたファイルを描いている画面**を `screens.mjs` の道から引きました。

| PR | 印（`data-design-node`）の数 | 印が無いときの引き方 |
|---|---|---|
| #550 | `sqFXf`×3 `cPk8A`×2 | — |
| #551 | `s7T2dz` `JCz6J` `W98zZQ` `s6Vvp` `PSmHo` 各1 | — |
| #552 | `WjYAC`×2 | `app/automations/page.tsx` を描く `gief7` `Vdbv5`、`new/page.tsx` を描く `Rv8Jv` |
| #553 | 印なし | `scenario-detail-client.tsx` の標準 `confirm` を持つ `dqFft` |
| #554 | 印なし | `broadcasts/page.tsx` の標準 `confirm` を持つ `EGMb1` |
| #555 | 印なし | `saved-view-dialog.tsx` の未入力エラーを出す `AuSDY`。同じ部品を使う `ANgda` `tBlkL` `LHjwD` も撮り直す |
| #556 | 印なし | `form-submissions/page.tsx` の帯を持つ `ZOPyc` |
| #557 | 印なし | `broadcast-kpi-values.tsx` を描く `q76C35` |

**#550〜#557 は、すべて画像で確かめました（2026-08-29）。** どれも head が
動いていないので、取り込まれた順に `--only` で変わった Node だけ
撮り直します。

**#555 は `tBlkL` の続きです。** P0（保存に失敗しても「保存しました」）は
#513 `60b39036` で直っており、#555 はそのあとの未入力エラーの出し方です。
**別のPRが同じファイルを触っているので、撮り直す順を間違えると
片方の直りが絵から消えます**（機能6で一度やりました）。

**#553 と #554 で、束5 の「ブラウザ標準の `confirm`」は
`zGZMA` を残すだけになります。** `zGZMA` は消す前に人数を出す必要が
あり、そのAPIが無いので束11のままです。

---

## 直す順（上から）

1. **束4のうちPRが無い4枚**（`ZOPyc` `q8wSqO` `HBTk0` `q76C35`）。
   `ZOPyc` は #436 の続きで、いちばん近い
2. **束5のうちPRが無い3枚**（`dqFft` `EGMb1` `zGZMA`）。
   `Y0Sn3` と `Gy9OK` の差分をそのまま写せる
3. **束3のうちPRが無い2枚**（`r7eSi` `p97Tf`）。言葉の置き換えだけ
4. **束10の `bPF0s` 予約を取り消す**。予約したのに止められないのは重い
5. **束9のうちPRが無い6枚**。直す量は小さく、見落とすと重い

**束7は着手しない。** 商品仕様の判断が先です。
