# S1(機能 1〜5)の「準備中・取得できません・内部語」一覧

- 日付: 2026-09-03
- 対象: 機能 1 ダッシュボード / 2 受信箱 / 3 友だち / 4 友だち属性 / 5 シナリオ配信(68 画面)
- 読んだ範囲: `apps/web/src` の S1 所有パスだけ。`.test.` と注釈は除く
- 数え方: **行ごとではなく文字列ごと**。コードの `null` や class 名を拾うと本当の違いが埋もれるため、
  文字列リテラルと JSX の地の文のうち**日本語を含むもの**だけを見た
- 撮影 head: `49e1341c`(codex/development を取り込んだあと)

## 1. まとめ

| 機能 | 準備中 | 取得できません | unavailable | 内部語 |
|---|---:|---:|---:|---:|
| 1 ダッシュボード | 0 | 6 | 0 | 1 |
| 2 受信箱 | 0 | 1 | 0 | 1 |
| 3 友だち | 3 | 0 | 0 | 3 |
| 4 友だち属性 | 0 | 4 | 0 | 0 |
| 5 シナリオ配信 | 4 | 2 | 0 | 5 |
| **合計** | **7** | **13** | **0** | **10** |

`unavailable` は**利用者に見える場所には 1 件も無い**。7 箇所あるが、すべて
注釈・CSS の class 名・状態の値(`status !== 'unavailable'`)で、画面には出ない。

## 2. 準備中(7 件)

S0 の `apps/web/src/app/no-preparing-contract.test.ts` の「減る一方の表」に載っている
7 ファイル 7 件と**過不足なく一致**した。第 2 段でここを消すと表の行も消える。

### 機能 3 友だち(3 件)

| 場所 | 文言 | 出方 |
|---|---|---|
| `app/friends/detail/page.tsx:319` | 一覧から隠す操作は準備中です | 無効ボタン「非表示」の吹き出し |
| `components/friends/bulk-run-dialog.tsx:345` | ほかの操作は準備中です。 | **本文に出る** |
| `components/friends/friend-timeline.tsx:152` | 印を付ける仕組みは準備中です | 無効チップの吹き出し |

### 機能 5 シナリオ配信(4 件)

| 場所 | 文言 | 出方 |
|---|---|---|
| `app/scenarios/page.tsx:233` | マニュアルは準備中です | 無効ボタン「マニュアル」の吹き出し |
| `app/scenarios/mode/page.tsx:200` | 同上 | 同上 |
| `app/scenarios/detail/scenario-detail-client.tsx:1348` | 同上 | 同上 |
| `app/scenarios/first-step/page.tsx:272` | 同上 | 同上 |

**6 件は吹き出し(`title`)にしか理由が無い。** 共通ルール §5-5 は「動かない操作は描かない、
どうしても位置を見せるなら理由を**本文の文字**で出す」なので、吹き出しは条件を満たさない。

## 3. 動かないのに置いてあるボタン(準備中と書いていないもの・2 件)

「準備中」の語が無いだけで、同じ形の問題。第 2 段でまとめて片づける。

| 場所 | 文言 | 対象 |
|---|---|---|
| `app/friends/detail/page.tsx:326` | ブロックはLINE側の操作です。管理画面からは変えられません | 無効ボタン「ブロック」 |
| `components/friends/advanced-search-dialog.tsx:556` | タグフォルダからまとめて指定する口がまだありません | 無効の指定口 |

S1 の担当範囲に `BlockedAction`(`not-connected.tsx`)の利用は **0 件**。上の 8 箇所
(準備中 6 + ここ 2)が、そのまま第 2 段の置き換え先になる。

## 4. 取得できません(13 件)

**13 件とも規則どおりで、直すものは無い。** 共通ルールは「取れない数字を 0 にしない。
未取得は `—` + 理由のラベル」と決めており、13 件すべてが理由のラベルとして使われている。

### 機能 1 ダッシュボード(6 件)

| 場所 | 文言 | 添えている値 |
|---|---|---|
| `app/page.tsx:236` | データを取得できませんでした。 | + 「もう一度読み込む」 |
| `app/page.tsx:615` | 取得できません | 値は `null` → `—` |
| `app/page.tsx:624` `628` `658` `662` | 取得できません | 同上(シナリオ・UID移行・予約・流入) |
| `app/page.tsx:745` | 一部のデータを取得できませんでした(…)。0件としては表示していません。 | 帯で明示 |

### 機能 2 受信箱(1 件)

- `components/chats/friend-info-sidebar.tsx:149` 友だち情報を取得できませんでした

### 機能 4 友だち属性(4 件)

- `app/tags/searches/edit/page.tsx:131` `148` `162` 友だち情報 / 対応マーク / シナリオを取得できません(選び口の空表示)
- `components/friend-fields/field-list.tsx:126` 使用中の数は取得できません(値は `—`)

### 機能 5 シナリオ配信(2 件)

- `app/scenarios/results/page.tsx:145` LINEでは友だち単位の開封を取得できません。…(仕様の説明)
- `app/scenarios/results/page.tsx:176` 配信失敗数は、この集計からは取得できません。(「エラー」は `—`)

## 5. 内部語(10 件)

横断レビュー §7 #48 の用語表と照らして、**運用者向けの文に混ざっている英語・機械語**を拾った。

| 機能 | 場所 | 文言 | 言い換え案 |
|---|---|---|---|
| 1 | `components/dashboard/dashboard-editor.tsx:78` | 右サイド｜Webhook・自動処理 | 「接続状態・自動処理」 |
| 2 | `app/chats/page.tsx:1558` | Flexメッセージ | 「カード」 |
| 3 | `app/duplicates/page.tsx:161` | アカウント別ブレイクダウン | 「アカウント別の内訳」 |
| 3 | `app/duplicates/page.tsx:198` | アカウント間 重複マトリックス | 「アカウント間の重複表」 |
| 3 | `components/friends/friend-timeline.tsx:63` | [Flex メッセージ] | 「カード」 |
| 5 | `scenario-detail-client.tsx:76` | Flex(カードタイプ) | 「カード」 |
| 5 | `scenario-detail-client.tsx:203` | 画像 JSON パースエラー | 「画像の設定を読み取れません」 |
| 5 | `scenario-detail-client.tsx:694` | Flex メッセージの JSON が不正です | 「カードの設定が壊れています」 |
| 5 | `scenario-detail-client.tsx:695` | 画像メッセージの JSON が不正です | 「画像の設定が壊れています」 |
| 5 | `components/scenarios/message-kind-fields.tsx:428` | パッケージ … / スタンプ … | 番号ではなく絵で見せる |

**「LINE Webhook」は数えていない。** ダッシュボードの接続状態カードに出るが、
設計 `vUXKb` そのものが「LINE Webhook」と書いており、正本は設計のため実装だけ直せない。
言い換えるなら Pencil が先(§7 #48 の範囲)。
