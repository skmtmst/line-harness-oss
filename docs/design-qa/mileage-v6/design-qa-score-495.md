# 行動スコアと手動調整 — PR #495 head `7d890d3b`

| | |
|---|---|
| 対象PR | [#495](https://github.com/skmtmst/line-harness-oss/pull/495) [V6] 行動スコア一覧と配信対象を接続 |
| head SHA | `7d890d3bc3daed8913fbc9b10701001930cb3c23`（**撮影直前に再確認。指定と同じ**） |
| 土台 | `codex/kenta-v6-feature17-adjustments`（#494）。**#494 → #441 を含みます** |
| 比較日時 | 2026-08-28 21:10 JST |

| Node | 画面 | ルート | 1440px | 1920px | 判定 |
|---|---|---|---|---|---|
| `z3PB2` | 17-2 行動スコア | `/mileage?tab=score` | [通常](z3PB2-normal-1440.png) / [読込](z3PB2-loading-1440.png) / [空](z3PB2-empty-1440.png) / [失敗](z3PB2-error-1440.png) | 同左（`-1920`） | **未実装 → 構造一致・データ未接続** |
| `vz0Ji` | 17-1-F 手で増やす・減らす | `/mileage/friends/detail?id=` の窓 | [OK](vz0Ji-1440.png) / [減らす](vz0Ji-decrease-1440.png) | [OK](vz0Ji-1920.png) | **要修正 P1**（#494 から据え置き） |

**横スクロールは 1440・1920 とも0です。** `z3PB2` は4状態とも撮れ、
`data-list-state` も `loading` / `empty` / `error` に分かれます。

---

# `z3PB2` 行動スコア — 未実装から外します

## 頼まれた確認、すべて通りました

| 見るところ | 結果 |
|---|---|
| 通常・読込・空・失敗 | **4つとも別の絵。** `data-list-state` も分かれる |
| 検索 | **効く**（「高橋」→1件。`search=高橋` が飛ぶ） |
| 絞り込み | **効く**（低い→2件、下がっている→2件。`filter=low` `filter=decreased`） |
| 並び順 | **効く**（`sort=score_desc` ほか5つ） |
| 表示件数 | **効く**（`limit` が変わる） |
| CSV | **落ちてくる**（`action-scores-2026-08-28.csv`） |
| 未取得 `—`／実値0 `0` | **分かれている**（下記） |
| 内部文言 | **出ない**（`API error:` も `Failed to fetch` も無し） |

## 未取得と実値0が分かれています

| 友だち | いまの点数 | 30日間の変化 | 最後の反応 |
|---|---|---|---|
| 山田 太郎 | 44 | **0** | **点数が変わった理由は未取得**／**—** |
| 新田 遥 | **0** | **0** | まだ行動がありません／2026/07/31 08:15 |

**「取れて0」と「取れない」が同じ列で読み分けられます。**
失敗のときは絞り込み札の数まで **`—`**（高い —／ふつう —／低い —／
下がっている —）になり、**0人と言い切りません。**

## スコア帯の引き継ぎが効きます

| 層 | 友だち一覧へ | 一斉配信へ |
|---|---|---|
| 高い | `/friends?scoreMin=70` | `/broadcasts/new?scoreMin=70` |
| ふつう | `/friends?scoreMin=40&scoreMax=69` | `/broadcasts/new?scoreMin=40&scoreMax=69` |
| 低い | `/friends?scoreMax=39` | `/broadcasts/new?scoreMax=39` |

**引き継いだ先でも条件が見えます。**

- 友だち一覧：「**行動スコア：40点以上〜69点以下**」
- 一斉配信：詳細条件が開き、`score_range` の行に **70** が入る
  （`targetMode` が `advanced` に切り替わる）

「すべて」を選んでいるあいだは案内文（「高い・ふつう・低いの層を選ぶと、
友だち検索と配信へ引き継げます。」）だけで、**ボタンは出ません。**
層を選んでから出る形です。

## 誤解を先に潰しています

> **スコアはマイルではありません。** 顧客には表示されず、反応の目安として
> 配信や対応の優先順位に使います。**顧客の価値を表すものではありません。**

設計に無い一文です。帯にも境目の点数が出ます
（高い〈70点以上〉／ふつう〈40〜69点〉／低い〈39点以下〉）。

## 残る差（P2）

| 差 | 中身 |
|---|---|
| 一斉配信側の入力の名札 | 70 が入っているのに `aria-label` が「**下限なし**」のまま。読み上げが値と食い違う |
| 並び順・表示件数 | 共通 `Select`（素の `<select>` ではない）。**素の `<select>` と混在している画面がある**（`LT8RS` 参照） |

**設計 `z3PB2` との突き合わせは、設計HTMLが未書き出しのため次回に回します。**
（`/Volumes/My Passport/Github/v6-design-ref/f17/z3PB2.html` は在るので、
次の比較で並べます。）

---

# `vz0Ji` 手動調整 — 「0未満にしない」は出ています

頼まれた確認です。

| 見るところ | 結果 |
|---|---|
| 「減らす」を選んだとき | **「残高より多くは減らせません。変更後の残高が0未満になる操作は実行しません。」が出る** |
| 「増やす」を選んだとき | 出ない（**正しい**） |
| 残高2,450に99,999を減らそうとしたとき | **確認の段へ進まない。**「利用可能な残高を超えて減らすことはできません」で止まる（`mileage-adjustment-dialog.tsx:95`） |
| 高額調整 | `approvalThreshold` を超えると「別のオーナー承認が必要です。」で止まる |
| 承認境界が未設定 | 「高額調整の承認境界が未設定です。オーナーが先に設定してください。」で止まる |

**止め方が4段あり、どれも文言つきです。**

## 据え置きのP1

**書き込みが失敗すると `API error: 405` と生で出ます**（#494 から変わらず）。
窓は開いたまま、入力も残るのは正しい形です。文字だけを日本語にしてください。

---

# 固定データ

`ACTION_SCORES`（`/api/action-scores/friends`）を型
（`ActionScoreOverview`）どおりに足しました。

- `lastReason` と `lastChangedAt` が **`null` の行を1つ**（山田 太郎）。
  未取得の `—` を撮るため
- `currentScore: 0` `change30d: 0` の行を1つ（新田 遥）。実値0を撮るため
- 層の数（高い2・ふつう2・低い2）は `scoredFriends: 6` に合わせてある

**口の当てはめで、絞り込み・検索・並び順・ページ送りを効かせました。**
効かせないと、押しても数が変わらず「効いていない」ように見えます。

## 空のときの形も足しました

`EMPTY_BODIES` に `/api/action-scores/friends` を足しました。
**一覧の既定（配列）を返すと画面が落ちます**（型は `summary` / `items` /
`pagination` を持つオブジェクト）。実際、直す前は
`z3PB2-empty` が2枚とも撮れませんでした。

層の数は**0で埋めています。** 空は「取れて0件」なので `—` にしません。

## 撮影の仕掛けに `'normal'` を足しました

`states.kinds` に `'normal'`（口を差し替えない、ふつうの絵）を
入れられるようにしました。**別の行にすると、状態の絵と本体の絵が
別々のheadになりうる**ためです。

final result（head `7d890d3b`）: `z3PB2` **未実装 → 構造一致・データ未接続**
（P0・P1とも0件、P2 2件）／`vz0Ji` **要修正 P1**（#494 から据え置き）
