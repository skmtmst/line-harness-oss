# ダッシュボードV6 画像比較（機能1）

**結論：未一致。5枚のうち2枚は未実装。ここに置いた画像は「合格した基準」ではありません。**

## 撮り方

| | 出どころ |
|---|---|
| 設計 | Pencil の実ノードを `html-css` で書き出し、Chromium で実寸描画（幅1920） |
| 実装 | 実ルート `/` を操作して 1440・1920 で撮影 |

**撮り方は `scripts/visual-qa/screens.mjs` にデータで書いてあります。**
機能ごとに使い捨てのスクリプトを書くと、262枚ぶんで必ず撮り方がずれ、
**ずれた絵どうしを比べて「差がある」と言ってしまいます。**

```
node scripts/visual-qa/capture-screens.mjs --check
node scripts/visual-qa/capture-screens.mjs --feature 1 --impl
node scripts/visual-qa/capture-screens.mjs --feature 1 --design --from <書き出したhtmlの置き場>
```

この5枚は、使い捨てスクリプトで撮ったものと**画素がまったく同じ**に
なることを確かめてから、仕組み側の絵に置き換えてあります。

**時計を止めて撮っています。** 「6日前」は `Date.now()` から出るので
（`pending-inbox-card.tsx` の `elapsed()`）、止めないと**日をまたぐたびに
絵が変わり**、基準画像として使えません。`2026-08-19T12:00:00Z` に固定し、
固定データの受信日時をその6日前・4日前に置いてあります。

## 突き合わせ

| Node | 画面 | 設計 | 実装 | 実装の場所 | 判定 |
|---|---|---|---|---|---|
| `vUXKb` | 1-1 ダッシュボード | [設計](../../design-reference/dashboard-v6/vUXKb.png) | [1440](vUXKb-1440.png) / [1920](vUXKb-1920.png) | `/` | **未一致** |
| `ZN0ov` | 1-1-1 ダッシュボード編集 | [設計](../../design-reference/dashboard-v6/ZN0ov.png) | [1440](ZN0ov-1440.png) / [1920](ZN0ov-1920.png) | `/` ＋「ダッシュボード編集」 | **未一致** |
| `JN6mQ` | 1-1-2 友だち追加QR | [設計](../../design-reference/dashboard-v6/JN6mQ.png) | [1440](JN6mQ-1440.png) / [1920](JN6mQ-1920.png) | `/` ＋「QRを表示」 | **未一致** |
| `NjK9q` | 1-1-3 対応受信の表示件数を開く | [設計](../../design-reference/dashboard-v6/NjK9q.png) | — | **無し** | **未実装** |
| `Alekb` | 1-1-4 通知パネルを開く | [設計](../../design-reference/dashboard-v6/Alekb.png) | — | **無し** | **未実装** |

1440・1920 とも横スクロールは0です。

## 未実装2件

### `NjK9q` 表示件数の切り替えが無い

設計は表の右上に「表示件数 5件表示」があり、開くと選べます。実装は
`pending-inbox-card.tsx` の `const PAGE_SIZE = 5` が固定で、**選ぶ場所が
ありません。**

同じ理由で `vUXKb` も欠けます。総数5件のうち2行を出して終わりで、**残り
3件へ行く手段がありません。** 設計は下に「1〜2 / 5件」とページ 1・2・>
が出ます。

### `Alekb` 通知パネルがダッシュボードに繋がっていない

設計は右上（「過去28日」の右）にベルとバッジ3があり、押すと通知パネルが
開きます。実装にベルはありません。

部品（`components/shared/notification-panel.tsx`）は在りますが、使われて
いるのは検証用ルート（`/visual-qa/friend-attributes?state=overlays-notification-panel`）
とテストだけで、**ダッシュボードからは呼ばれていません。**

## `vUXKb` の差

### 右カラムに2枚足りない

| 設計にあるカード | 中身 |
|---|---|
| **現在の対応マーク** | 未対応 360人／対応済み 38人／メッセージ受信時の自動変更：有効 |
| **友だちの状態** | 友だち総数 621人／有効 398人／ブロック・非表示 223人（35.9%）／内訳 相手から223人・自分から0人・相互に0人 |

どちらも実装にありません。

### 送信枠の帯が2か所に出る

設計は右上の「今月の送信枠」に1つだけです。実装は下の「今月の配信」にも
「送信枠 3 / 200・残り98.5%」を出していて、**同じ数を2回**描いています。

### 文言の差

| 場所 | 設計 | 実装 |
|---|---|---|
| 状態の札 | 正常稼働（緑） | 状態確認中（灰） |
| 送信枠の見出し | 毎月1日リセット | 月初リセット |
| 待ち時間 | 最長 6日前 | 最長 6日7時間50分 |
| 今日の予約の副文 | 変更・取消 0 | 変更・取消を含む予約一覧 |
| 出荷予定の副文 | 未処理なし | 今日・明日 0件 |
| 今後の予定 | 予定されている配信・予約はありません | 予定されている**予約**はありません |
| 運用アラート | 2件／最も古い未対応：9,110分前／二段階認証：0 / 6人 | 0件／接続・自動処理：正常です／未対応の最長待ち：6日7時間50分 |

**二段階認証の行が実装にありません。**

### 設計に無いものが出ている

画面のいちばん下に「2026/1/13 7:00:00 時点 ・ 最新データへ更新」が出ます。
設計にこの行はありません。

## `ZN0ov` ダッシュボード編集の差

### パネルが全高でない

設計は画面の上端から下端までの縦長パネルで、**カード20枚が1画面に並びます**。
実装は高さが1010pxほどで途中で切れ、「接続状態」から下が見えません。
設計にある 友だちの状態／今後の予定／今月の配信／最近の成果／予約状況／
流入経路TOP3／ファネル要注意／オートメーション失敗 に、そのままでは届きません。

### 「4枠まで」の注意が一行に縮んでいる

設計は黄色い箱で書いてあります。

> 「今日やること」は4枠までです
> 5つ目をONにすると、いちばん下のカードが自動でOFFになります。順番を入れ替えて、先に出したい4つを上に置いてください。

実装は見出しの右に「最大4枚まで・ドラッグで順番変更」の一行だけです。
**ONにしたときに何が起きるかが読めません。**

### 行の副文が「位置」から「中身」に変わっている

| | 設計 | 実装 |
|---|---|---|
| 対応が必要な受信 | 上部・小カード | LINE・メールの未対応 |
| 出荷予定 | メイン・横長 | メイン｜横幅いっぱい |
| 友だち追加リンク | メイン・左カラム | 追加URL・QRコード |

設計は**どこに置かれるか**を書いています。並べ替える画面なので、位置が読めないと
動かした結果が予想できません。

### そのほか

| 場所 | 設計 | 実装 |
|---|---|---|
| 見出しの下 | 表示するカードと位置を変更します | （無い） |
| 「初期状態に戻す」 | タブと同じ行の右端 | タブの上に単独で左寄せ |
| タブの形 | 塗りつぶしの札（緑） | 下線タブ |
| 保存ボタン | ダッシュボードに反映 | 変更を適用 |
| パネル幅 | 約540px | 約440px |

## `JN6mQ` 友だち追加QRの差

### QR画像が出ない（取得不能）

実装の絵では、QRの場所に壊れた画像の枠と alt（友だち追加QRコード）が出ます。
**こちらの固定データがQR画像を返していないため**で、実装の差とは言い切れません。
画像を返せるようにしてから撮り直します。

### 形式タブの並びと見出し

| | 設計 | 実装 |
|---|---|---|
| 見出し | ダウンロード形式 | 形式 |
| 並び | PNG / JPG / SVG | PNG / **SVG / JPG** |

### ボタン

| | 設計 | 実装 |
|---|---|---|
| 主ボタン | ⬇ 画像をダウンロード（緑・アイコン付き） | PNGをダウンロード（白・アイコン無し） |
| 印刷 | 🖨 PDFで印刷（アイコン付き） | PDFで印刷（アイコン無し） |

主ボタンが白いので、**この画面でいちばんやりたいことが目立ちません。**

### そのほか

- QRの下：設計は「然-NEN- 公式」＋リンク `https://lin.ee/nen-official`。実装は名前だけで**リンクが無い**
- 「使うときのヒント」：設計は ⓘ 付きの**枠付きの箱**。実装は区切り線の下にベタ書き
- リンク欄：設計は枠の中で2行に折り返し、コピーは文字のリンク。実装は1行で省略され、コピーは緑のボタン
- ダイアログ幅：設計 約820px、実装 約765px

## 判定を保留にした3点（こちらの固定データが薄いだけかもしれない）

`/api/accounts/:id/health` を `{ riskLevel: 'ok', logs: [] }` としか返して
いないため、次の3つは実装の差か固定データ不足かを**まだ切り分けられません**。
切り分けてから判定します。**いまは「実装が違う」とは書きません。**

| 場所 | 設計 | いまの実装 |
|---|---|---|
| 状態の札 | 正常稼働 | 状態確認中 |
| LINE Webhook | 3 / 3 正常 | 確認中 |
| 自動処理 | 稼働中 | 要確認 |

## 設計そのものの食い違い（1件）

`vUXKb` の中で数が合いません。

- 「接続状態」の**有効友だち 4人**
- 「友だちの状態」の**有効 398人**

同じ `friends.active` から出る値なので、両方は描けません。ここでは398で
返しています。**設計側の直しが要ります。**

## 固定データについて

全部0で返していたあいだ、ダッシュボードは空の絵しか描けませんでした。
受信の表もページ送りも送信枠の帯も出ないので、**設計と並べても「差が無い」
とは言えません**（そもそも比べる中身がない）。設計の絵に書いてある値を
`mock-api.mjs` に入れてから撮っています。

途中で1つ落とし穴がありました。`/api/support/inbox` を
`{ items, total }` の形で返したところ、画面が読むのは `{ items, summary }`
だったため、**行が1つも描かれないのにページ送りだけ出る**という、実際には
起きない絵になりました。型に照らして直してあります。

final result: **未一致（要修正）** — 未実装2・未一致3
