# 機能23 EC連携 — Issue #396 再判定

## 比較対象

- 正本: `docs/design-reference/ec-v6/{eI3gs,ELayY,bfB50,oHAN4}.txt`
- 実装画像: `docs/design-qa/ec-v6/<node>-<state>-{1440,1920}.png`
- CSS viewport / deviceScaleFactor: 1440×1080 / 1920×1080、1
- 実画像: eI3gs・ELayY・bfB50 は 1440×1080 / 1920×1080、oHAN4 は全面 1440×1179 / 1920×1179
- 状態: 通常・読込・空・失敗・権限不足。ELayY は判定窓も確認
- 設計画像: 4 node ともなし。プロジェクト規則に従い `.txt` 正本と同幅の実装画像を照合した

## Findings

- eI3gs: 注文・個別処理の実API接続後、注文金額、商品明細、処理結果、失敗行の再試行がそろった。match。
- ELayY: 集計、候補の根拠、確からしさ、影響、判定窓が実APIと固定データで再現できた。match。
- bfB50: 構造と状態は一致。月別の開始・停止集計と離脱兆候の契約がなく、取得値を作らず data pending。
- oHAN4: 構造と状態は一致。下流5機能への影響件数APIがなく、「— 未取得」のため data pending。

フォント・字級・行高、カードと表の余白、色トークン、角丸、状態色を4画面の通常画像で確認した。画像資産は正本・実装とも使用していない。1440/1920pxの全画像で横はみ出しは0。通常画像で見切れや重なりはなく、長い定期便名は列内で省略される。文言は運用者向けで、内部ID・外部イベントID・秘密値・平文PIIを表示しない。

全体画像で主要なカード、タブ、説明帯、表、操作を読めるため、追加の部分拡大は不要と判断した。

## 比較履歴

- 前回: eI3gs は出来事詳細API待ち、ELayY は集計API待ち、bfB50・oHAN4 は設計画像なしで未判定。
- 今回の修正: 注文一覧・個別処理・会員照合候補の実API接続と固定データを反映。
- 再撮影: 3106/8793、全50枚、横はみ出し0。eI3gs・ELayY は match、残る2画面は不足契約を明記して structure_match_data_pending。

## 結論

Issue #396 の範囲に、未解消の視覚欠陥はない。bfB50・oHAN4 の残差は未取得データの契約であり、値を作らない判断を維持する。

final result: passed
