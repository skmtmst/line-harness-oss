# 未実装画面の片づけ方

`scripts/visual-qa/screens.mjs` の `gap` から機械で組み立てています。**手で書き写していません。**

未実装 **64** 枚を片づけ方ごとに分けました。

| 分け方 | 枚数 | 何を指すか |
|---|---|---|
| **既存部品で作れる** | 1 | もうリポジトリに在るものを当てるだけ。`ConfirmDialog` `ListState` `Select` など |
| **通常実装** | 3 | 画面を新しく作る。**口（API）は既に在る**ので、つなぐだけ |
| **新規API・DBが必要** | 40 | 記録・集計・走らせる仕掛けが無い。**先に決めることがある** |
| **V6から除外候補** | 7 | 作らない決めがある、またはほかの画面に統合済み。**判断をお願いします** |
| **Codex実装中・新PR待ち** | 13 | 作っているところ。**headが届いたら撮ります** |

## 既存部品で作れる（1枚）

もうリポジトリに在るものを当てるだけ。`ConfirmDialog` `ListState` `Select` など

| 機能 | Node | 画面 | 何が要るか |
|---|---|---|---|
| 8 自動応答 | `Gy9OK` | 8-1-I 削除確認 | 既存 `ConfirmDialog` にルール名、新しい受信への応答と後続処理が止まること、過去の実行履歴は残ることを表示する。`auto_reply_hits` は外部キーを持たず削除後も残るため新規DBは不要 |

## 通常実装（3枚）

画面を新しく作る。**口（API）は既に在る**ので、つなぐだけ

| 機能 | Node | 画面 | 何が要るか |
|---|---|---|---|
| 6 一斉配信 | `cPk8A` | 6-1-B 対象条件 | 保存した検索の仕組みは #421 で入った。配信側から呼ぶだけ |
| 6 一斉配信 | `sqFXf` | 6-1-L 対象条件を編集 | 同上 |
| 25 オートメーション | `WjYAC` | 25-1-C 見本から作る | 見本は固定データでよい。ただし実IDを埋めず、選択した見本から公開ではなく下書きを作り、タグ・シナリオ・テンプレートを利用者が選び直す。V6オートメーション実装チェーンの作成API上に載せる |

## 新規API・DBが必要（40枚）

記録・集計・走らせる仕掛けが無い。**先に決めることがある**

| 機能 | Node | 画面 | 何が要るか |
|---|---|---|---|
| 17 マイル・行動スコア | `qlVLJ` | 17-1-B マイルの使い道 | 交換の定義（何と何マイルで替えるか）と、引き換えの記録が要る |
| 17 マイル・行動スコア | `p9CcEB` | 17-1-G マイルの使い道をつくる | 同上 |
| 20 分析 | `URqOA` | 20-1-D 定期レポートをつくる | 決まった曜日・時刻に走らせる仕掛けと、送り先の保存が要る |
| 22 写真審査 | `hHrz8` | 22-1-A 写真を1枚ずつ見る | 一覧写真を拡大するだけでは完成しない。非公開original、review/public派生画像、crop/rotateの版、risk flag、同意、審査競合、original download権限を持つasset・decision API/DBが要る |
| 22 写真審査 | `J3Wxl8` | 22-1-C 出しているもの | 通した写真と掲載先（リッチメニュー・コラム・サイト）を結ぶ記録が要る |
| 3 友だち | `IAf7j` | 3-1-C 友だち（一括アクション） | 複数人へまとめて タグ・シナリオ・担当者・対応マーク・リマインダ・メッセージ を反映する口が無い。画面も別ページとして作る必要がある |
| 3 友だち | `InCDe` | 3-2-A 重複候補詳細・統合前確認 | 集計口だけでは作れない。候補ID、友だち2件、根拠、確信度、pending/linked/different/deferred/invalidated、判定者・理由・履歴を持つ `identity_candidates` と候補詳細・判定APIが要る |
| 3 友だち | `w8W4Eh` | 3-3-A 統合ユーザー詳細 | 元の友だちを残した非破壊リンク、解除履歴、項目ごとの採用値と出所、配信優先順位を扱う `friend_identity_links`・`user_profile_values`・`user_delivery_priorities` と詳細APIが要る |
| 3 友だち | `vtBCu` | 3-4 UID移行 | 異なるLINEプロバイダー間のUID自動変換はできない。検証済み対応表の取込、dry-run、競合判断、本移行、照合、切り戻しを持つ `uid_migration_runs/items` とowner・二者確認APIが要る |
| 4 友だち属性 | `GMvBd` | 4-3-A 対応マークを追加・編集 | **既存部品だけでは作れません。** 設計は1枚の画面で、名前・色・並び順・初期値のほかに**「自動変更ルール」**（担当者を割り当てたときなど、きっかけでマークを自動で変える）を持つ。ここに新しい口とテーブルが要る。名前・色・並び順・初期値までなら既存部品で作れる |
| 7 リマインダ | `s7T2dz` | 7-1-C 対象と終了条件 | 終了・停止条件の保存先が要る |
| 7 リマインダ | `JCz6J` | 7-1-D 配信予定プレビュー | 設定だけを画面側で計算せず、`POST /api/reminders/{id}/preview` で各通の予定日時、過去で作られない通、条件で外れる通、取消時に止まる通を返す必要がある |
| 7 リマインダ | `W98zZQ` | 7-1-E テスト送信確認 | リマインダ専用のdry-run／テスト送信APIが要る。実登録、予定、送信後アクション、履歴、回数制限を更新せずにプレビューと送信結果を返す |
| 7 リマインダ | `s6Vvp` | 7-1-F 最終確認 | 公開版、`validate`、`preview`、`publish` が要る。基準日・通・テスト・対象・終了条件をサーバーで確認し、公開する版を固定してから有効化する |
| 7 リマインダ | `PSmHo` | 7-1-G 有効化完了 | `publish` の返事（公開版、対象、予定件数、最初の実行、監視先）を受けて完了画面を出す。最終確認のAPI契約と同時に作る |
| 8 自動応答 | `U9hzqH` | 8-1-D 競合と優先順位 | `GET /api/auto-replies/conflicts` とsimulateが要る。候補、実際に勝つルール、勝つ理由、優先順位変更後の差を同じ評価器から返す |
| 8 自動応答 | `g46ja` | 8-1-E 自動応答テスト | `POST /api/auto-replies/test` のdry-runが要る。評価順、一致・不一致理由、勝ったルール、返信、実行予定アクション、抑止理由を返し、状態を更新しない |
| 8 自動応答 | `Yj6CQ` | 8-1-F 最終確認 | 下書き・validate・conflicts・test・publishが要る。競合とループ防止をサーバーで確認し、公開版を固定してから有効化する |
| 8 自動応答 | `e6iJG` | 8-1-G 有効化完了 | `publish` の返事（公開版、優先順位、監視、競合解消結果）を受けて完了画面を出す。8-1-Fと同じ契約で作る |
| 9 友だち追加時の配信 | `ec9vg` | 9-1-F 最終確認 | `friend-add-rules` のdraft、validate、conflicts、test、publishと冪等キーが要る。流入条件・初回案内・アクション・二重送信防止を確認し、公開版を固定する |
| 9 友だち追加時の配信 | `quhg6` | 9-1-G 有効化完了 | `publish` の返事（公開版、対象見込み、二重送信防止、監視先）を受けて完了画面を出す。9-1-Fと同じ契約で作る |
| 10 ウェビナー | `Ho8z4` | 10-1-D 通知・リマインド | 前日・1時間前・開始時に走らせる仕掛けが要る |
| 11 テンプレート | `NNDMR` | 11-1-C 質問を作る | 「質問」の型と保存先が要る。いまの `messageType` に無い |
| 12 リッチメニュー | `szXsT` | 12-1-F リッチメニューの削除確認 | 確認窓だけでは作れない。現在表示中の人数、次に表示されるメニュー、切替元、自動応答・オートメーション等の参照元を返す影響確認と、LINE取り下げの完了を保証する実行記録が要る。`force=true` は管理画面へ出さない |
| 13 回答フォーム | `gBp2J` | 13-1-E フォームの削除確認 | 確認窓だけでは作れない。削除前にフォーム名・公開状態・回答数・利用中の場所・開けなくなるURLを返す影響確認が要る。公開中・回答あり・利用中は物理削除せず、停止・保管へ移す契約と `status` / `deleted_at` が必要 |
| 14 共通情報 | `uNBlA` | 14-1-B 変える前に影響を見る | 共通情報の差し込み先を1件ずつ引く口が要る |
| 14 共通情報 | `yPkWe` | 14-1-C 共通情報の削除確認 | 確認窓だけでは作れない。使用先と版、変更前後の文、予約中・公開中を返す影響確認、互換種類の代替候補、全使用先の差し替え、旧定義のアーカイブ、実行結果の記録が要る。#437の削除影響APIは件数と種類別集計までで、V6の差し替えは未実装 |
| 15 登録メディア | `YfTfJ` | 15-1-C メディアの削除確認 | 確認窓だけでは作れない。使用先の完全な台帳、差し替え互換性の事前確認、一括差し替え、アーカイブ、未使用・未公開・履歴なしだけを削除するジョブが要る。現行の使用先取得と409停止は安全側だが、V6の「別の画像に差し替え」はまだ実行できない |
| 16 成果とアフィリエイト | `njLGA` | 16-1-C 支払い | 締め日・支払日・振込先・未払い残高の表が要る |
| 16 成果とアフィリエイト | `GqFTV` | 16-1-H 支払いを確定する | 同上（締める操作） |
| 18 流入と計測 | `UIaM7` | 18-1-E 流入リンクの削除確認 | 確認窓だけでは作れない。投稿・広告・自動処理・成果対応の使用先、過去流入数、転送先候補を返す影響確認と、停止・アーカイブ・別リンクへの転送を行う契約が要る。履歴がある経路は物理削除しない |
| 21 NEN配信 | `ymXJK` | 21-1-E コラムを書く | コラムの正本はEC側。管理画面で書くなら保存先が要る |
| 23 EC連携 | `ELayY` | 23-1-A 会員のつき合わせ | `friendId` が空のeventを並べるだけでは足りない。検証済みemail/電話の候補、account/shop境界、linked/rejected/deferred判断、影響確認、過去eventの再処理範囲を持つidentity候補・判断API/DBが要る |
| 23 EC連携 | `bfB50` | 23-1-B 定期便 | Stripe定期便本体の接続。実装に「接続後に有効化します」と明記 |
| 23 EC連携 | `oHAN4` | 23-1-C EC連携のつなぎ先 | つなぎ先と突合キーを画面から変える口が要る。実装に「口が無い」と明記 |
| 24 LINE通知 | `X8JCA5` | 24-1-B 送れなかったもの | 届かなかったお知らせを残す記録が要る |
| 24 LINE通知 | `Se65i` | 24-1-C お知らせの記録 | いつ・だれに・どのお知らせを送ったかの記録が要る。読む元は ec_events と messages_log(source=ec_transactional)。**開封は作らない** |
| 24 LINE通知 | `DpxOK` | 24-2 運用者へのお知らせ | 運用者へ知らせる仕掛けそのものが要る |
| 24 LINE通知 | `N2gAza` | 24-2-A 運用者へのお知らせをつくる | 同上（作る画面） |
| 26 外部連携 | `KNG00` | 26-1-B やり取りの記録 | 外部連携のやり取りを残す記録と、やり直しの口が要る |

## V6から除外候補（7枚）

作らない決めがある、またはほかの画面に統合済み。**判断をお願いします**

| 機能 | Node | 画面 | 何が要るか |
|---|---|---|---|
| 9 友だち追加時の配信 | `s9gAx` | 9-1-A 基本設定 | 設定はアカウントに1枚。名前もフォルダも優先順位も要らない |
| 9 友だち追加時の配信 | `W1wzCa` | 9-1-B 流入条件 | 実装に「流入元の記録は友だち追加のたびに必ず走るので、ここでは選びません」と明記 |
| 9 友だち追加時の配信 | `K0Dbr2` | 9-1-C 初回案内 | 最初に送る本文はシナリオ側にある。**2か所に持つと必ず食い違う** |
| 9 友だち追加時の配信 | `Q3qP1r` | 9-1-I 削除確認 | 設定は1枚で消せない。削除という考えがそもそも無い |
| 10 ウェビナー | `LKuAQ` | 10-1-K 削除確認 | 物理削除はV6要件の除外対象。公開停止・アーカイブへ置き換え、申込・視聴・分析・監査は保持する。現行 `webinarApi.remove` は視聴履歴を物理削除する一方、申込記録を削除対象に含めず孤児化させるため、画面へそのまま接続しない |
| 13 回答フォーム | `ava2n` | 13-1-B フォームのデザイン設定 | **作らない決めが実装に明記**。「見た目をこのアプリのデザインにそろえる方針にしたため、色やフォントを選ぶ画面は作っていない」 |
| 16 成果とアフィリエイト | `QX70l` | 16-1-G アフィリエイターを削除する確認 | 物理削除確認はV6から除外する。紹介者は停止・アーカイブし、過去成果・承認・未払い・支払い記録を保持する。個人情報削除は識別情報の匿名化として別要件にする |

## Codex実装中・新PR待ち（13枚）

作っているところ。**headが届いたら撮ります**

| 機能 | Node | 画面 | 何が要るか |
|---|---|---|---|
| 5 シナリオ配信 | `RUxNf` | 5-1-I シナリオ・配信開始確認 | #521 head `7d5d74fd` で開始・停止の確認窓を実装済み。最新headを1440/1920で比較してから未実装を外す |
| 5 シナリオ配信 | `NrBkW` | 5-1-J シナリオ・配信開始完了 | #522 head `3c88b8bd` で開始完了と実績確認への導線を実装済み。取り込み順は #521 → #522 |
| 6 一斉配信 | `bPF0s` | 6-1-I 予約完了 | #510 head `ab1841bc` で予約完了画面を実データへ接続済み。#497を土台にしているため、取り込み順は #495 → #497 → #510。最新headを1440/1920で比較してから未実装を外す |
| 7 リマインダ | `Y0Sn3` | 7-1-I 削除確認 | #498で未送信だけを取消し、送信済み履歴を残すsoft deleteと確認窓を実装済み。#514で一部失敗時に成功分と残りを分けて再試行可能にする。取り込み順は #498 → #514 |
| 9 友だち追加時の配信 | `P2J0Te` | 9-1-H 実行結果 | #506 head `5dc99107` で既存 `/api/friend-add-routing/events` を読む `/friend-add-settings/runs` を実装済み。最新headを1440/1920で比較してから未実装を外す |
| 10 ウェビナー | `GB0NR` | 10-1-F 公開ページプレビュー | #507 head `579fa25c` で実URLの公開ページを別窓で開く導線を実装済み。画像確認待ち |
| 10 ウェビナー | `D6yO7e` | 10-1-G 公開前確認 | #508 head `61eeb3c7` で動画・配信枠・長さを検査し、公開内容を読む確認窓を実装済み。取り込み順は #507 → #508 |
| 10 ウェビナー | `TimXl` | 10-1-H 公開完了 | #508 head `61eeb3c7` で公開完了画面と公開後の導線を実装済み。取り込み順は #507 → #508 |
| 12 リッチメニュー | `DIUbO` | 12-1-C 切替メニューのつながり | #509 head `e148615c` で既存のpages/areasから切替のつながりを解析する `/rich-menus/connections` を実装済み。最新headを1440/1920で比較してから未実装を外す |
| 12 リッチメニュー | `NXdDk` | 12-1-C-A つながりなし | #509 head `e148615c` の切替解析画面に、つながりが無い場合の状態を実装済み。空の固定データで画像確認する |
| 27 予約管理 | `GFDqW` | 27-1-D 代理予約・内容確認 | #459 head `ba0bf62d` に入力→内容確認→登録完了→競合回復の4段が実装済み。`confirm` 状態を操作して画像確認する |
| 27 予約管理 | `GfceK` | 27-1-E 代理予約・登録完了 | #459 head `ba0bf62d` の `done` 状態に実装済み。登録成功レスポンスを返して画像確認する |
| 27 予約管理 | `Lg8ff` | 27-1-F 代理予約・予約枠の重なりと入力エラー | #459 head `ba0bf62d` で予約枠の重なり判定と `slot_conflict` / `slot_not_available` の回復画面を実装済み。競合レスポンスで画像確認する |
