# 機能設定V6 画像比較（機能31）

**結論：1枚とも撮れています。判定は未一致です。**

**この画面は設計にとても近い形で作られています。**

## 突き合わせ

| Node | 画面 | 実装 | 判定 |
|---|---|---|---|
| `c4R6F` | 31-1 機能設定 | [1440](c4R6F-1440.png) / [1920](c4R6F-1920.png) | **未一致**（下に4点） |

1440・1920 とも横スクロールは0です。

## 合っているところ

- 区分ごとの箱（基本／配信／コンテンツ／成果と分析／自動化／予約／専用機能／設定）
- 「グループごと切替」
- 「必須」の錠前（基本と設定の項目は消せない）
- 右の並びのプレビュー
- 「初期値に戻す」「保存」
- オフにしてもデータは残る、という説明

実装の説明はむしろ設計より具体的です。

> オフにしても、その機能で作ったデータ（タグ・配信履歴・予約など）は
> 削除されません。**APIも動いたままなので、管理画面から隠れるだけです。**

## 差（4点）

| # | 設計 | 実装 |
|---|---|---|
| 1 | 並べ替えは「並びを変える」の別画面（「並び順はここでは変えません」） | ↑↓ をその場に置く（「並び順は↑↓で、同じ区分の中だけ入れ替えられます」） |
| 2 | 「同じスイッチ」の注記（共通情報・登録メディア一覧・成果とアフィリエイト・コンバージョン・予約管理・予約設定） | 無い |
| 3 | 「飲食店向け（テスト）」の区分（9機能） | **出てこない** |
| 4 | 設定の区分に「アカウント」「データ移行」 | **メニューそのものに無い**（`menu.ts` の設定区分は3項目） |

### 1番は、実装のほうが良いと思います

設計は並べ替えを別画面に分けますが、実装は同じ画面で ↑↓ を押せます。
**行き来が1回減ります。** 設計に合わせて分ける理由は見当たりません。

### 3番について

`feature-settings.ts:146` に、はっきり書いてあります。

```
.filter((group) => group.items.length > 0 && group.id !== 'restaurant-test')
```

**わざと外しています。** ただし `menu.ts` にはこの区分があり、
サイドバーには出ます。つまり **サイドバーに出ているのに、
機能設定からは消せない区分がある**状態です。

検証用の区分なので外している、という判断はありえます。
ただ**出ているのに消せない**のは、使う人からは説明が付きません。

### 4番について

設計の設定は5項目（アカウント／ログインユーザー／機能設定／データ移行／運用状態）、
実装は3項目です。「アカウント」（LINEアカウントの登録と切り替え）と
「データ移行」（UIDなど既存データの移行状況）が、
**メニューにありません**（`/accounts` の画面自体は在ります）。

アカウントの切り替えは画面の右上にあるので困りませんが、
**登録・追加へ行く道がサイドバーから消えています。**

## 固定データで1つ直しました

`/api/settings/features` の `specializedFeatureKeys` が空でした。
空だと `visibleFeatureGroups` が「専用機能」の区分をまるごと隠すので、
**設計より短い画面が撮れ、実装に無いように見えます。**

`nen_campaigns` `photo_review` `ec_commerce` `line_notifications`
`restaurant_test` を渡すようにしました。

final result: **未一致（要修正）** — 未一致1
