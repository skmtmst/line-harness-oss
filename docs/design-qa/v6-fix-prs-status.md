# 出した指摘に、もう修正PRが出ているか（2026-08-29 03:40 時点）

open のPRを読み、**こちらが挙げた欠けに当たる差分が出ているか**を調べました。
**マージはしていません。差分を読んだだけです。**

---

## P0

| こちらの指摘 | Node | 修正PR | 差分を読んだ結果 |
|---|---|---|---|
| 保存に失敗しても「保存しました」と出る | `tBlkL` | **#513** `60b39036` | **そのとおり直っている。** `onSave` が `SavedViewSaveResult` を返し、`if (!result.success)` を見てから `setDone(true)` へ進む。失敗の文は窓の中に出る。`savedViewCreateError.message` の素通しもやめている。契約試験も付いている |
| 読込失敗を空として出す（回答フォーム） | `ZOPyc` | **無し** | **まだ出ていません。** `codex/development` `2e438929` の `form-submissions/page.tsx:128` は `catch { /* silent */ }` のまま |

---

## 束1「失敗を空として出す」

| Node | 画面 | 修正PR | 差分を読んだ結果 |
|---|---|---|---|
| `f8SBSh` | 26 外部連携 | **#515** `09054b78` | `ListState` を当て、`kind="error"` を足している。**前回の一覧を残さない**注記付き |
| `Vdbv5` | 25 オートメーション | **#516** `a8c9d2a7` | 同上。`loadStatus === 'ready' ? automations.length : '—'` で未取得と0を分けている |
| `q5G45` | 5 シナリオ配信 | **#519** `a8e00234` | 同上 |
| `bzDn6` | 3 友だち | **#520** `4848a8f3` | 同上。件数も `'—'` に落としている |
| `ZOPyc` | 13 回答フォーム | **無し** | **P0。いちばん先に要ります** |
| `h8pBZr` | 15 登録メディア | **無し** | |
| `W6465r` | 28 予約設定（メニュー） | **無し** | #517 は `/booking/staff`（担当スタッフ）で、こちらとは別の画面 |
| `k5m5Bc` | 29 イベント予約 | **無し** | #518 は `/events/bookings`（申込一覧）で、こちらとは別の画面 |

**4つ出て、4つ残っています。** 残る4つのうち `ZOPyc` は P0 です。

---

## そのほか、こちらの記録に関わるもの

| PR | head | 何をしているか | 関係するNode |
|---|---|---|---|
| #504 | `a7aeda33` | 顧客向けLINE通知の履歴と失敗一覧。`ec-commerce.ts` と `line-notifications/page.tsx` を触っている | `Se65i` `X8JCA5`（残り4画面のうち2枚） |
| #506 | `5dc99107` | 友だち追加時配信の実行結果を既存台帳へ接続 | 機能9 |
| #509 | `e148615c` | リッチメニューの切替つながりを実データで確認 | 機能12 |
| #510 | `ab1841bc` | 一斉配信の予約完了を実データへ接続 | `bPF0s` 6-1-I（いま未実装） |
| #511 `#512` | `4bc71249` `feb2d543` | リマインダの実行結果を読みやすく／配信予定を実行台帳へ | `GC4St` の P1×5 |
| #514 | `9a72dba6` | リマインダの一部削除失敗を再試行可能にする | 機能7 |
| #517 | `43d3d20e` | 予約スタッフの読込失敗を空表示と分ける | 機能28（`W6465r` とは別画面） |
| #518 | `56793b6a` | イベント予約の読込失敗を空表示と分ける | 機能29（`k5m5Bc` とは別画面） |
| #521 | `7d5d74fd` | シナリオ開始・停止を確認してから実行する | `RUxNf` 5-1-I（いま未実装） |
| #522 | `3c88b8bd` | シナリオ開始完了を実績確認へつなぐ | `NrBkW` 5-1-J（いま未実装） |
| #507 `#508` | `579fa25c` `61eeb3c7` | ウェビナーの公開前確認・公開完了・公開ページ確認 | 機能10 |

---

## 撮り直しは、まだしていません

**どのPRも取り込まれていないので、判定は動かしていません。**
判定は「こちらが撮った head での結果」です。差分が入ってから、
**変わったNodeだけ** `--only` で撮り直します。

撮り直すには、そのPRの木で画面確認サーバを起こす必要があります。
いま動いているサーバは `v6-f18-443`（`6db5ad7f`）の木です。

**機能ごと撮り直さないでください。** #503 の head で機能6を丸ごと
撮り直したとき、#497 で直した `FpgxH` を直る前に戻しました。
気づいて git から戻しましたが、気づかなければ「直っていない」と
報告するところでした。

---

## いま Codex にいちばん頼みたいこと

1. **`ZOPyc` 回答フォーム**（P0）。#515・#516・#519・#520 と同じ形で片づきます
2. `h8pBZr` 登録メディア、`W6465r` 予約設定（メニュー）、`k5m5Bc` イベント予約
3. `API error: 500` の素通し4枚（`RW5Tb` `zCQXe` `W6465r` `k5m5Bc`）
4. `h0kahp` テスト送信が**押すたびに配信を1件残す**（`broadcast-form.tsx:514`）
5. 内部の言葉8枚（`cron` `silent rule` `line_account_id` `carousel` ほか）
