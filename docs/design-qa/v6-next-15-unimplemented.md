# 未実装の次の15枚 ― 5つに分ける

**書いた日**：2026-08-30 ／ [削除確認の段1](v6-delete-confirm-step1-spec.md) の続き

未実装は33枚。うち8枚は前の文書で扱いました。ここは**残り25枚から15枚**です。

**共通の見どころ**（どの画面にも当てはまるので、以下では繰り返しません）

- 通常・読込・空・失敗、権限が要る画面は権限不足も別々に出す
- **未取得は `—`、数えて0だけ `0件`**
- 1440px・1920pxで、ページ・表・内側の要素とも横スクロール0
- `undefined` / `Invalid Date` / `NaN` / 内部ID / `API error` を出さない

---

## 分類4：V6から除外する候補（7枚）

**7枚とも、実装側に「作らない理由」が書かれています。** こちらで勝手に決めず、**除外の可否だけ判断してください。**

| Node | 画面 | 実装に書かれている理由 |
|---|---|---|
| `s9gAx` | 9-1-A 基本設定 | 設定はアカウントに1枚。名前もフォルダも優先順位も要らない |
| `W1wzCa` | 9-1-B 流入条件 | 「流入元の記録は友だち追加のたびに必ず走るので、ここでは選びません」 |
| `K0Dbr2` | 9-1-C 初回案内 | 最初に送る本文はシナリオ側にある。**2か所に持つと必ず食い違う** |
| `Q3qP1r` | 9-1-I 削除確認 | 設定は1枚で消せない。削除という考えがそもそも無い |
| `LKuAQ` | 10-1-K 削除確認 | 物理削除はV6要件の除外対象。公開停止・アーカイブへ置き換える |
| `ava2n` | 13-1-B デザイン設定 | 「見た目をこのアプリのデザインにそろえる方針にしたため、色やフォントを選ぶ画面は作っていない」 |
| `QX70l` | 16-1-G 紹介者の削除確認 | 物理削除はV6から除外。停止・アーカイブし、過去成果と支払い記録を残す |

**`LKuAQ` と `QX70l` には注意が要ります。** 「作らない」ではなく「**別の形にする**」です。停止・アーカイブの画面は要ります。除外するなら、**代わりの画面をV6に足す**必要があります。

**`LKuAQ` は現行実装に危うさがあります。** `webinarApi.remove` は視聴履歴を物理削除する一方、申込記録を消さずに残すため**孤児になります**。除外の判断より先に、この口を画面へつながないでください。

**決めていただくこと**：7枚を262から外すか。外すなら `LKuAQ` `QX70l` の代替2枚を足すか。

## 分類5：親PR待ち（1枚）

| Node | 画面 | 待つもの |
|---|---|---|
| `D6yO7e` | 10-1-G 公開前確認 | **#508** `61eeb3c7`（CLEAN）が実装済み。ただし **#507 が `DIRTY`** |

取り込み順は **#507 → #508**。#507 の競合が解けたら、`D6yO7e` だけ `--only` で撮ります。**こちらの作業は要りません。**

## 分類3：API/DBが必要（7枚）

### 自動応答の4段 ― `U9hzqH` `g46ja` `Yj6CQ` `e6iJG`

| | |
|---|---|
| ルート | `/auto-replies`（段は `?stage=` で分ける） |
| 触るファイル | `app/auto-replies/page.tsx`、`components/auto-replies/`（新規） |
| 重なるopen PR | #258 #430 #450 #491 #544 #566 |
| 既存資産 | **手本が完成しています** |

**#551 がリマインダでまったく同じ4段を作りました。** `s7T2dz`（対象と停止条件）→ `JCz6J`（届く予定）→ `W98zZQ`（テスト送信）→ `s6Vvp`（最終確認）→ `PSmHo`（公開完了）の5枚を確認済みです。

- 口：`POST /api/reminders/drafts` → `GET/PUT /:id/draft` → `/preview` → `/validate` → `/test-send` → `/publish`
- 画面：`components/reminders/reminder-publish-flow.tsx` の1枚で `?stage=` を切り替える

**自動応答も同じ形にしてください。** 違うのは中身だけです。

- `U9hzqH` 競合と優先順位 → リマインダの「届く予定」に当たる。**同じキーワードに複数のルールが当たるとき、どれが動くか**を先に見せる
- `g46ja` テスト → `W98zZQ` と同じ。**本番の記録もヒット数も動かさないことを画面に書く**
- `Yj6CQ` 最終確認 → `s6Vvp` と同じ。公開する版を固定する
- `e6iJG` 有効化完了 → `PSmHo` と同じ。数が最終確認と食い違わないこと

**受入条件**：#551 と同じ5項目（版の固定／チェックの言い分け／未取得は `—`／完了の数が確認と一致／テストが本番を動かさずそう書く）。

**修正単位**：**1本のPRに4段まとめて**。段だけ先に作ると、契約が半分の状態で残ります。**ただし #566 が取り込まれてから**着手してください（同じ `page.tsx` を触ります）。

### `w8W4Eh` 3-3-A 統合ユーザー詳細

| | |
|---|---|
| ルート | `/friends?tab=merged`（詳細） |
| 触るファイル | `app/users/page.tsx`、`components/users/` |
| 重なるopen PR | **#565**（一覧を作ったPR。詳細は対象外と本文に明記） |
| 新規API・DB | 要ります。元の友だちを残した非破壊リンク、解除履歴、項目ごとの採用値と出所、配信優先順位（`friend_identity_links` / `user_profile_values` / `user_delivery_priorities`） |

**#565 の「詳細を見る」は、いま**同じ行の下を開くだけ**です（新しい口は呼びません）。設計の詳細画面はその先です。**#565 が取り込まれてから**着手してください。

### `hHrz8` 22-1-A 写真を1枚ずつ見る ／ `J3Wxl8` 22-1-C 出しているもの

| | |
|---|---|
| ルート | `/nen-members` |
| 触るファイル | `app/nen-members/page.tsx` |
| 重なるopen PR | #258 **#447** #535 |
| 新規API・DB | 要ります。1枚ずつの審査（承認・却下・理由）と、公開しているものの一覧 |

**#447 が機能22の土台**（写真審査の理由・通知・公開同意）で、`DIRTY` ではありません。**#447 → #535 の順**で取り込んでから着手してください。

---

## まとめ：着手の順

| 順 | Node | 待つもの | 修正単位 |
|---|---|---|---|
| — | `D6yO7e` | #507 の競合解消 → #508 | **実装不要**（撮るだけ） |
| 1 | `hHrz8` `J3Wxl8` | #447 → #535 | 2枚で1本 |
| 2 | `U9hzqH` `g46ja` `Yj6CQ` `e6iJG` | #566 | **4枚で1本**（#551 を写す） |
| 3 | `w8W4Eh` | #565 | 1本 |
| — | 除外候補7枚 | **判断待ち** | — |

**除外の判断が先に要ります。** 7枚を外すと未実装は33→26枚になり、残りの見通しがはっきりします。
