# 未実装67枚 — 口とテーブルの要りようを、機能をまたいで束ねる

最終更新：2026-08-28

数は `scripts/visual-qa/screens.mjs` から機械で出しています
（`node scripts/visual-qa/ledger.mjs --gaps`）。

```
未実装 67枚 ＝ 新規API・DB 24／通常実装 22／既存部品 16／除外候補 5
```

**機能ごとに見ると24件の別々の宿題に見えます。** 束ねると **7つ**です。
いちばん大きい束が7枚を占めていて、**Codexがいまその1枚目
（`GC4St` リマインダの実行結果）を作っています。**

---

## A. 実行の記録 — **7枚。いちばん効きます**

| 機能 | Node | 画面 |
|---|---|---|
| 7 | `GC4St` | リマインダの実行結果 ← **いま作っているところ** |
| 8 | `t7UtYQ` | 自動応答の実行結果 |
| 5 | `M2b2B` | シナリオの配信結果 |
| 24 | `Se65i` | お知らせの記録 |
| 24 | `X8JCA5` | 送れなかったもの |
| 25 | `DkPY0` | オートメーションが動いた記録 |
| 26 | `KNG00` | 外部連携のやり取りの記録 |

### 設計は、もう同じ表です

`DkPY0`・`KNG00`・`Se65i` の設計を並べると、列がほぼ一致します。

| 列 | `DkPY0` | `KNG00` | `Se65i` |
|---|---|---|---|
| 日時 ／ 相手 | 8/24 14:02 ／ 佐藤 千尋 | 8/23 16:30 ／ こちらで受け取った | 8/24 18:00 ／ 林 里佳 |
| どのアカウント | LINE 本店 | アンケートツールから | LINE 本店 |
| きっかけ | 「予約」と送られたとき | 成果が認められたとき | お荷物を送りました |
| 参照 | — | 受け取り口 2 | #12466 |
| 結果 | 動きました／動きませんでした | 200 OK／人が見つかりません | 届きました／届きませんでした |
| かかった時間 | 0.4秒 | 0.4秒 | — |
| 追記 | メニュー切替 ／ 回答フォーム送信 | 満足度 5 | 15:21 に開きました |
| 操作 | 中身を見る／もう一度やる | 中身を見る／やり直す | 中身を見る／もう一度送る |
| ページ送り | 9,660件中 1〜7件 | 1,972件中 1〜7件 | 3,826件中 1〜7件 |

`X8JCA5` は**同じ表を失敗だけに絞ったもの**で、理由の列
（ブロックされています／友だちではありません／結びついていません）と
代わりの手段（受信箱で連絡／メールを送る）が増えます。

`GC4St` と `t7UtYQ` は表ではなく**まとめの面**で、稼働状況・要確認
（送信エラー 2件）を出し、「実行結果を確認」で同じ表へ入ります。

`M2b2B` だけは1件ずつではなく**割合**（開始386人・完了68.4%・
ステップ別の開封/クリック）ですが、**同じ記録から数えられます。**

### すでに同じ形が1つあります

`packages/db/bootstrap.sql` の **`automation_runs`** が、
そのまま実行の記録の形をしています。

```
line_account_id      どのアカウント
friend_id            相手
source_event_id      きっかけの元
idempotency_key      二重に数えない
status               queued / running / waiting / success / partial
                     / failed / cancelled / skipped_condition
input_event_json     「中身を見る」で出すもの
is_test              テストかどうか
started_at           かかった時間はこの2つから引ける
completed_at
resume_at            待っているものの再開時刻
```

`automation_run_steps` には `attempt_number` と `idempotency_key` があり、
**「もう一度やる」も既にこの形で数えられます。**

**足りないのは1つだけです。** `automation_id` が
`automation_definitions` に固定されているので、
**リマインダ・自動応答・シナリオ・お知らせ・外部連携が入りません。**

`messages_log` は同じ問題を **`origin_kind` + `origin_id`** で
先に解いています（`packages/db/bootstrap.sql`）。同じやり方で
`owner_kind` + `owner_id` + `owner_version_id` を持たせれば、
**7枚が1つの表と1つの口に乗ります。**

読む側は1本で足ります。

```
GET /api/execution-runs?ownerKind=reminder&ownerId=…&status=failed&limit=20&offset=0
```

### 効きめ

- **7枚が1つの基盤に乗ります。** 機能ごとに作ると表が7つ、口が7つ
- **「動かなかった理由」を最初から持てます。** `DkPY0` の設計は
  「動きませんでした／何もしていません」を出します。`status` に
  `skipped_condition` が既にあるので、後から足す必要がありません
- **1枚だけ既にできています。** `P2J0Te`（9-1-H 友だち追加時配信の実行結果）は
  `/api/friend-add-routing/events` があるので「通常実装」に分類してあります。
  **形が使えることの証拠です**

### 気をつけること

- **未取得と0を分ける。** 「送信エラー 0件」と「まだ数えていない」は別。
  `X8JCA5` は「送れなかったもの 4件」、`GC4St` は「停止予定 0件」と
  実値で書かれています
- **「もう一度やる」は取り消せない操作です。** 確認の窓（`H2S1T4`）と
  `idempotency_key` の両方が要ります
- **かかった時間を持たない画面もある**（`Se65i`）。列を必須にしない

---

## B. 決まった時刻に走らせる — 3枚

| 機能 | Node | 画面 | 要るもの |
|---|---|---|---|
| 10 | `Ho8z4` | ウェビナーの通知・リマインド | 前日・1時間前・開始時に走らせる |
| 20 | `URqOA` | 定期レポートをつくる | 決まった曜日・時刻に走らせ、送り先を保存する |
| 7 | `s7T2dz` | リマインダの対象と終了条件 | 終了・停止条件の保存先と、その判定 |

**3枚とも「時計で動くもの」です。** `event_booking_reminders` と
`mileage_event_queue`（`available_at` で待つ形）が既にあるので、
**どちらかに寄せられるか先に見てください。** 3つ目の仕掛けを増やすと、
止まったときにどれを見ればよいか分からなくなります。

走った結果は **Aの記録に入れてください。** 別に持つと、
「送られたはずのものが来ていない」を追う場所が2つになります。

---

## C. お金の締め — 2枚

| 機能 | Node | 画面 |
|---|---|---|
| 16 | `njLGA` | 支払い（締め日・支払日・振込先・未払い残高） |
| 16 | `GqFTV` | 支払いを確定する |

**2枚で1つです。** 締めた後は書き換えられない形（版を固定する）が要ります。
`action_score_rule_versions`（#496）と同じ考え方です。

**ここは急がなくてよいと思います。** 成果とアフィリエイトの残り7枚は
撮れていて、支払いだけが外に出ています。お金が動く前に決めることが
多いので、ほかが片づいてからで足ります。

---

## D. マイルの使い道 — 2枚

| 機能 | Node | 画面 |
|---|---|---|
| 17 | `qlVLJ` | マイルの使い道 |
| 17 | `p9CcEB` | マイルの使い道をつくる |

交換の定義（何と何マイルで替えるか）と、引き換えの記録の2つ。
**引き換えの記録は Aと同じ形に乗ります**（相手・日時・結果・参照）。
定義のほうだけが新しい持ちものです。

`mileage_adjustments`（#494）で「誰がいつ何マイル動かしたか」は
既に残るようになっているので、**引き換えもそこへ寄せられないか
先に見てください。**

---

## E. 運用者へのお知らせ — 2枚

| 機能 | Node | 画面 |
|---|---|---|
| 24 | `DpxOK` | 運用者へのお知らせ |
| 24 | `N2gAza` | 運用者へのお知らせをつくる |

`notification_center`（migration 197）が既にあります。
**まず何が入っているか見てください。** 足りないのは
「作る側」だけかもしれません。

送った結果は **Aの記録**、送る時刻は **Bの仕掛け**に乗ります。

---

## F. 外部の正本につなぐ — 3枚

| 機能 | Node | 画面 | 正本はどこ |
|---|---|---|---|
| 23 | `bfB50` | 定期便 | Stripe |
| 23 | `oHAN4` | EC連携のつなぎ先 | EC側 |
| 21 | `ymXJK` | コラムを書く | EC側 |

**3枚とも「正本が外にある」ものです。** 決めることは同じで、

- どちらを正本にするか
- 突き合わせる鍵は何か
- 食い違ったときどちらを勝たせるか

`bfB50` と `oHAN4` は**実装に「接続後に有効化します」「口が無い」と
書いてあります。** 意図して置いてあるので、勝手に埋めないでください。

`ymXJK` は**管理画面で書く必要があるかから決める話**です。
EC側で書くなら、V6から外す候補になります。

---

## G. その画面だけの持ちもの — 5枚

束ねられないもの。1枚ずつです。

| 機能 | Node | 画面 | 要るもの |
|---|---|---|---|
| 4 | `GMvBd` | 対応マークを追加・編集 | **自動変更ルール**（きっかけでマークを変える）の口とテーブル |
| 11 | `NNDMR` | 質問を作る | 「質問」の型と保存先。いまの `messageType` に無い |
| 14 | `uNBlA` | 変える前に影響を見る | 共通情報の差し込み先を1件ずつ引く口 |
| 27 | `Lg8ff` | 代理予約・予約枠の重なり | 予約枠の重なりを判定する口 |
| 22 | `J3Wxl8` | 出しているもの | 通した写真と掲載先（リッチメニュー・コラム・サイト）を結ぶ記録 |

`GMvBd` は**8月28日にここへ移しました。** それまで「既存部品で作れる」と
していましたが、設計が自動変更ルールを持つので、部品だけでは作れません。

`J3Wxl8` は Aに近い形（対象・時刻・結果）ですが、**残すのは「出来事」では
なく「いま出ているものの結びつき」**なので、別に置いてあります。

---

## 残り43枚の見取り

### 通常実装 22枚 — 口は在る。画面だけ

このうち **6枚が同じ形**です。

> `NrBkW` 配信開始完了 ／ `bPF0s` 予約完了 ／ `PSmHo` 有効化完了 ／
> `e6iJG` 有効化完了 ／ `quhg6` 有効化完了 ／ `TimXl` 公開完了

**保存の返事を受けて「終わりました」の面を出すだけ**で、
どれも口は在ります。1つ作れば残り5つは写せます。

### 既存部品 16枚 — 確認の窓

**12枚が同じファイルを触る未統合PRにふさがれています**
（[v6-parts-12-confirm-blocked.md](v6-parts-12-confirm-blocked.md)）。
取り込みを待って、上から実装します。
`Y0Sn3`（リマインダ削除確認）は Codex が送信履歴を残す安全削除と
一緒に作っています。

### 除外候補 5枚

4枚が機能9（友だち追加時の配信）で、**実装に「作らない理由」が
書いてあります。** 設計から外すかどうかの判断だけが残っています。

---

## 進める順（おすすめ）

1. **A の実行の記録** — 7枚が乗る。いま `GC4St` を作っているので、
   **そのとき `owner_kind` を持たせるかどうかだけ決めてください。**
   後から入れると7枚ぶん書き直しになります
2. **通常実装の「完了の面」6枚** — 口が在るので、画面だけ
3. **既存部品の確認の窓** — ふさいでいるPRが取り込まれた順に
4. **B の時計で動くもの3枚** — 既にある2つの仕掛けに寄せられるか先に見る
5. **E の運用者へのお知らせ** — `notification_center` に何が入っているか先に見る
6. **D → F → C → G** — 決めることが多い順に後ろへ

**Claude側はここまでです。** 口とテーブルは Codex の担当なので、
実装はしません。画面が出てきたら、その head で1440px・1920px で
比較して台帳へ反映します。
