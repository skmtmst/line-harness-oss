# w8W4Eh 統合ユーザー詳細 V6比較

- 対象: Issue #411
- 設計: `docs/design-reference/friends-v6/w8W4Eh.png`
- 実装: `docs/design-qa/friends-v6/w8W4Eh-normal-1920.png`
- 追加操作: `docs/design-qa/friends-v6/w8W4Eh-profile-1920.png`
- 確認幅: 1440px / 1920px
- 表示倍率: 100%

## 比較結果

全画面では、上段3カード、結び付いている友だち、統合された属性、横断履歴、主要操作の位置と情報の順序を比較した。設計と同じ骨組みで、両幅とも横はみ出しはない。

プロフィール編集では、項目ごとの採用値と今後の更新方法を選べる。候補はマスク済み表示だけで、候補ID、友だちID、LINEアカウントID、生のメールアドレスと電話番号は画面へ出ない。古い候補を保存した場合は409として最新状態の再読込を案内する。

通常、読込中、0件、取得失敗、権限不足、プロフィール編集、配信元編集、解除確認、版競合を撮影した。壊れ値と内部IDの露出はない。

## 判定

final result: passed

`w8W4Eh` は一致。以前未接続だった項目ごとの候補値選択は、候補IDをサーバー側で再照合して保存する実APIへ接続した。
