# 次にCodexへ渡す10件（API不要・同じファイル・open PRと非重複）

**日付**：2026-08-30 ／ **台帳**：`scripts/visual-qa/screens.mjs`（262枚）
**この文書はアプリを変更しない。**

## 選びかた（機械で出した）

1. `verdict: 'needs_fix'` の187枚から、**推奨修正に「口が要る／新しい表／未接続／集計の口」が無いもの**を取る
2. Nodeのルートから**触るファイル**（`apps/web/src/app/<route>/page.tsx`）を決める
3. open PR 144本の**変更ファイル一覧**と突き合わせ、**そのファイルを触っているPRが1本も無い束**だけ残す

**ディレクトリ単位では絞れない。** open PRが144本あり、ほぼ全ディレクトリを覆う。
**同じファイルを触るかどうか**まで見て初めて、重ならない束が出てくる。

重ならない束は5つ。**うち3つ・10件**を選んだ。

---

## 選んだ10件

### 束A：`scenarios/detail/page.tsx`（6件）

**この1枚に6つのNodeが同居している。** 1枚ずつ直すと同じ画面を6回触ることになる。

| Node | 設計 | 直すこと |
|---|---|---|
| `hz9ti` | 5-1-F 送信後アクション | **P1** 選べる動作が8つのうち5つ。テキスト送信・テンプレート送信・リマインダ操作・イベント予約操作が無い |
| `EvVO5` | 5-1-H 開始条件 | **P1** きっかけ6種類・初回のみ/毎回・一致人数の窓が無い |
| `dqFft` | 5-1-G 通の削除確認 | **差は無い。手本として残す**（どの通か・何が一緒に消えるか・何が残るか） |
| `r6Gzsu` | 5-1-E 配信条件 | **P2** 上の3つと同じ1枚。段へ分ける |
| `xfYLn` | 5-1-D 通の編集 | **P2** 分岐の作りと送信後アクションの並び |
| `bV5Vs` | 5-1-C シナリオ編集 | **P2** 配信対象の列と、注意帯「作成しただけでは配信されません」 |

**新しい口は要らない。** 動作の順番と条件分岐は**共通アクション（`py5CG`）が既に12種類の処理を持っている**ので、その作りを写せる。人数を数える口は **`cPk8A`（配信の対象条件）が既に持っている**。

**先にやること**：`hz9ti` の足りない4動作 → `EvVO5` の人数表示 → 段へ分ける。
**押したあとに戻せない操作なので、人数は押す前に見せる。**

### 束B：`booking/bookings/new/page.tsx`（3件）

| Node | 設計 | 直すこと |
|---|---|---|
| `GfceK` | 27-1-E 登録完了 | **P1** リマインダの具体時刻と「成果地点『予約が入った』を1件 数えました。**電話の予約も同じように数えます。**」を出す |
| `GFDqW` | 27-1-D 内容確認 | **P2** 重なりの事前警告（「9/02(火) 11:00 は 佐々木 がふさがっています」） |
| `Lg8ff` | 27-1-F 重なりと入力エラー | **差は無い。手本として残す**（`ApiError.code` で機械コードと人へ見せる文を分ける） |

**新しい口は要らない。** 重なりの確認は **`Lg8ff` が登録後に既に扱えている**ので、同じ空き確認の口を確認の段でも読むだけ。リマインダの時刻は **`JCz6J`（リマインダの予定計算）を使い回せる**。

**なぜ効くか**：**代理で入れた予約がLINEの予約と同じ扱いになる、という設計の要点が `GfceK` でしか確かめられない。** ここが出ないと、電話の予約が今日の件数に入っているのか運用者が判断できない。

`cpdDi`（電話の予約を入れる）は**同じファイルだが外した**——LINEにいない人を受ける顧客台帳が要るので、この束では終わらない。

### 束C：`scenarios/page.tsx`（1件）

| Node | 設計 | 直すこと |
|---|---|---|
| `RUxNf` | 5-1-I 配信開始確認 | **P1** 配信前チェック4項目（開始条件・配信タイミング・テスト送信・送信枠） |

**`s6Vvp`（リマインダの最終確認）が同じ形の公開前チェック5項目を既に持っている**ので、その部品を写すだけ。人数は `EvVO5` と同じ口。

**束Aと同じ機能なので、まとめて1本のPRにできる。**

---

## 選ばなかった束と理由

| 束 | 件数 | 理由 |
|---|---|---|
| `tags/new/page.tsx` | 3 | **設計画像が無い。** `l25rlp` `tP0RW` `LfrQs` は文章の記述だけで見ており、**具体的な差を書けない**。画像が用意できてから |
| `mileage/friends/detail/page.tsx` | 2 | `HIU5O` は失効ロットの口が要る。`vz0Ji` は差が無い |
| `cpdDi` | 1 | 顧客台帳が要るので、画面だけでは終わらない |

---

## 渡しかたの提案

- **束A＋束Cを1本**（機能5・7件）、**束Bを1本**（機能27・3件）
- どちらも **`--only` で撮れる**ので、こちらの確認は1回ずつで済む
- 束Aは**段へ分ける作業が中心**なので、`PV1Vh` `d3rFGD` `Xjk8q`（ウェビナー編集）と同じ形。**どちらかで作った段の部品を写せる**

## この文書でしていないこと

アプリ・Worker・DB・migration・APIの変更、Ready化、マージ、base変更、force push。
