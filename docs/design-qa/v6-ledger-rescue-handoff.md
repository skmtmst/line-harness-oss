# 台帳の正本候補と、2026-09-02 の撮影

この枝 `backup/ledger-rescue` は **保管用**。Ready化・マージはしない。
#492・#672 には書き戻していない。

---

## 1. なぜこの枝が正本候補なのか

`scripts/visual-qa/screens.mjs` が3か所にあり、**中身が違う**。

| 場所 | 大きさ | 行数 | 状態 |
|---|---|---|---|
| `codex/development` | 25,895 B | **95行**（機能1〜8だけ） | 本流。**262枚ぶんは入っていない** |
| `codex/kenta-v6-feature21-recheck`（**PR #492・OPEN**） | 461,293 B | 262行 | 未マージ |
| 外付けドライブの作業コピー `c650f0af` | 475,117 B | 262行 | **GitHubに存在しなかった** |

`docs/design-qa/v6-screen-ledger.md` も本流には無い。

つまり **262枚ぶんの Node・判定・撮影head は、本流のどこにも無い。**
この枝は「#492 の先頭 `f5e43417`」＋「外付けから救出した `c650f0af` の中身」なので、
**いま最も欠けが少ない版**になっている。

### 救出の経緯

外付けドライブ（`/Volumes/My Passport`）が作業中に段階的に壊れた。

| 時刻(JST) | 症状 |
|---|---|
| 17:33 | `git checkout` が10分で1ファイルも書けず `U`（I/O待ち）で停止 |
| 17:40 | 書き込み **2.7 MB/s**、`ls` に **1.7秒** |
| 18:20 | `git push` が `.git/objects/…: Operation not permitted` で失敗 |
| 18:25 | 通常ファイルの `cp` も `Operation not permitted` |

`c650f0af`「台帳に書いた『既にある』が実在しなかったのを訂正」は
`gh api .../commits/c650f0af` が **422（No commit found）**を返した＝未push。
commit ごとの push はドライブの障害でできなかったため、
**読めるうちに取り出せた `screens.mjs` だけ**を `f5e43417` の上に載せた（345追加/273削除）。

**取り出せなかったもの**：同じコミットにあったかもしれない
`docs/design-qa/v6-screen-ledger.md`（remote 側の版は残っている）。
それ以外に未pushの変更があったかは、ドライブが読めないため確認できていない。

以後、外付けドライブへの読み書きは停止している。

---

## 2. 2026-09-02 の撮影

- **撮影head：`df3f4e3b`**（= #665 のマージコミット）
  - 第1群5本 #666 #667 #668 #670 #674 と、第2群4本 #660 #661 #664 #665 を含む
- 撮影環境：**内蔵SSDのクローン**（外付けは使っていない）
  - `pnpm install` → `pnpm -r --filter "!web" build` → `apps/web/scripts/build-release-log.mjs`
  - `node scripts/visual-qa/mock-api.mjs`（`/api/accounts` と `/api/broadcasts/stats` が 200 を確認）
  - `NEXT_PUBLIC_API_URL=http://127.0.0.1:8788 pnpm --filter web dev --port 3101`

### 撮れたもの（32ファイル＝16 Node × 1440/1920）

機能6 `broadcasts-v6`（12 Node）
`q76C35` `zZ9fA` `XQfMD` `p97Tf` `Bw0zt` `h0kahp` `vW4Es` `FpgxH` `bPF0s` `u6gHt` `EGMb1` `xkRDb`

機能4 `friend-attributes-v6`（7 Node ※ `XBkiQ` は台帳を直してから撮った）
`l25rlp` `tP0RW` `LfrQs` `ee0sk` `byqIW` `A1ZYeP` `XBkiQ`

### 撮れなかったもの

| Node | 理由 |
|---|---|
| `cPk8A` | 「保存した条件から選ぶ」が押せない（無効のまま）。先に対象の選び方が要るのか、未実装かは未確認 |
| `sqFXf` | 「この条件を保存」が押せない。条件を組んでからでないと押せないと思われる |
| `TmHjF` | 口の返事を差し替えて撮る形。いまの仕組みに差し替えの手順が無い |
| `VjXGX` | 「保存する」が見つからない（0個）。固定データにその行やボタンが出ていない |
| `zGZMA` | 「削除」が見つからない（0個、2番目が要る）。同上 |

### 撮っていないもの（台帳の都合）

**#666（機能18）・#667（機能15/16/17）・#668（機能14/16）の対象機能は、
本流 `codex/development` の95行版に行が無い。**
#670 の主対象 `rIhbN`・`QKx8Q`・`hqrOv` も同じ理由で撮れていない。
撮影は本流の木で行ったため、この枝の262行版ではなく95行版が使われた。

→ **次に撮るときは、この枝の262行版を使う。**

---

## 3. 判定を書き換えた Node

**`q76C35` の1件だけ。** ほかは「撮っただけ」で判定は据え置いている
（絵を1枚ずつ見ていないため。見ていない絵の判定を書き換えない）。

### `q76C35`（6-1 一斉配信）で確かめたこと

`broadcasts-v6/q76C35-1440.png`（`df3f4e3b`）を見た結果：

- **解決：列が設計どおりの6列になった**
  タイトル・内容／状態／配信条件／配信日時／配信・開封・クリック／操作。
  以前の注記にある「実装は8列」「1列ずれ」は解消している。
- **解決：まだ送っていない配信の「配信・開封・クリック」が `0` ではなく `—`**（下書き2件・予約済み1件で確認）
- **解決：帯に `undefined` が出ていない**（今月の配信 12件／到達 1,842通／平均開封率 69.4%／失敗 0通）
- **確認：配信日時は日本時間で固定**
  `apps/web/src/app/broadcasts/page.tsx:37` が `timeZone: 'Asia/Tokyo'` を明示。撮影機は UTC+7。

**要確認（判定を据え置いた理由）**
帯の4枚が「今月の配信／到達／平均開封率／失敗」に戻っている。
上の #602 の注記は「予約中・下書き `—`・今月の配信・平均開封率」にしたと書いている。
**どちらが設計かをこの絵からは決められない。**
1機能を複数PRで直しているので、撮り直しで別PRの直しが戻った可能性がある。

---

## 4. 台帳そのものの食い違い（本流95行版で見つけた）

本流の95行版は `XBkiQ` を
`route: '/tags?tab=searches'` / `status: 'unimplemented'` /
「条件の確認・編集の画面が無い」としていた。

実際には `apps/web/src/app/tags/searches/edit/page.tsx` が実在し、
`/tags/searches/edit?id=ss-1` は **HTTP 200** を返す（#670 がこのファイルを直した）。

**この枝の262行版は、はじめから正しいルートと判定を持っていた。**
95行版のほうが古い、という裏づけの1つ。

---

## 5. この枝に入っていない、進行中の作業

| PR | 内容 | 状態 |
|---|---|---|
| [#676](https://github.com/skmtmst/line-harness-oss/pull/676) | 集計できていない数を0と書かない（分析・流入と計測・運用状態） | Draft・CI pass |
| [#677](https://github.com/skmtmst/line-harness-oss/pull/677) | LINEユーザーIDを画面から消す・詳細条件の消えていた軸を出す | Draft・CI直し済み |

どちらも `codex/development` 直結（`2f91d4c1`）。この保管枝とは別。
