# 未確認5画面を0にしました（2026-08-28）

**「未確認」は、こちらが確かめていないという意味です。** 実装が無いことを
確かめれば「未実装」、あることを確かめれば撮ります。5枚とも決着しました。

| Node | 画面 | ルート | 前 | いま | 確かめ方 |
|---|---|---|---|---|---|
| `vW4Es` | 6-1-G 配信前チェック | `/broadcasts/new` | 未確認 | **撮れる** | 本文を書いたら帯が埋まった |
| `FpgxH` | 6-1-H 最終確認 | `/broadcasts/new` | 未確認 | **未実装** | `save()` を直に呼ぶ。`ConfirmDialog` が0件 |
| `bPF0s` | 6-1-I 予約完了 | `/broadcasts/new` | 未確認 | **未実装** | 成功すると一覧へ戻るだけ |
| `GB0NR` | 10-1-F 公開ページプレビュー | `/webinars/edit` | 未確認 | **未実装** | `disabled` 直書き＋「準備中です」 |
| `ava2n` | 13-1-B フォームのデザイン設定 | `/form-submissions/edit` | 未確認 | **未実装（除外候補）** | 「作っていない」と覚え書きに明記 |

---

## `vW4Es` は実装がありました

前回「置き文のまま」と書いたのは、**こちらの口が
`POST /api/broadcasts/preflight` を405で弾いていたため**でした。
**数えるだけで何も保存しない口**なので通すようにしたところ、
本文を書いた時点で帯が埋まりました。

```
配信前チェック   2件 未確認
1,284 人に届きます
本文の文字数は問題ありません   24文字。分割されずに1通で届きます。
ブロック中の友だち 42人を除いています
同じ本文の配信を、この7日で1回送っています
本文にURLはありません          URLを入れると自動で短縮し、クリックを記録します。
テスト送信がまだです            本番前に自分宛に1通送って、見え方を確認してください。
開封数を集計できます            対象1284人。20人以上なので LINE 側で開封数が集計されます。
```

**「2件 未確認」と数えたうえで、何が未確認かを1行ずつ出しています。**
20人未満なら「開封数は集計されません」に変わる作りです。

`mock-api.mjs` は**保存と配信の口をこれまでどおり405で返します。**
通したのは数えるだけの1本だけです。

## `FpgxH` `bPF0s` は、押した瞬間に確定します

```tsx
<button disabled={saving} onClick={() => void save()}>
  {saving ? '保存中…' : sendMode === 'scheduled' ? '配信を予約する' : '下書き保存'}
</button>
```
（`broadcast-form.tsx:1001-1007`）

`/broadcasts` 配下に `ConfirmDialog` は**1つもありません。**
成功すると `router.push('/broadcasts')` で一覧へ戻るだけです
（`broadcasts/new/page.tsx:55`）。

**1,284人へ送る操作に、確認の段がありません。** 設計は6-1-H（最終確認）と
6-1-I（予約完了）でそこを止めています。

## `ava2n` はV6から外す候補です

```tsx
{/* 設計にあるが、まだ作っていないもの。並びから消すと「この画面には
    その機能が無い」ように見えるので、押せない状態で置いておく。
    デザイン設定は、フォームの見た目をこのアプリのデザインに
    そろえる方針にしたため、色やフォントを選ぶ画面は作っていない。 */}
```
（`form-submissions/edit/page.tsx:375-378`）

**作らない決めが、理由つきで書いてあります。** 設計側から外すかどうかの
判断をお願いします。

---

## ついでに見つけたもの：`KoT6c` も実装済みでした

`4-2-B 友だち情報欄・項目移行`は「画面もルートも無い」と書いていましたが、
**#420（head `87c150ad`）で `/tags/fields/migrate` が入っていました。**
使用中の項目（`usageCount > 0`）の行にだけ「移行」が出ます
（`field-list.tsx:143`）。使っていない項目は消せるので出ません。

**未確認を潰すついでに、未実装の1枚も外れました。**
