# 機能8-1-H 自動応答の実行結果（`t7UtYQ`）— PR #501

| | |
|---|---|
| PR | [#501](https://github.com/skmtmst/line-harness-oss/pull/501) |
| head | `93edbe17e2aaab4bcb25ac1d8e59c082598ede86`（**撮る前に確認済み**） |
| base | `codex/kenta-v6-execution-ledger`（#500 `409f00bb`） |
| ルート | `/auto-replies/runs?id=rule-a` |
| 口 | `GET /api/auto-reply-runs?ruleId=rule-a` |
| 撮った日 | 2026-08-28 |
| 幅 | 1440px・1920px |

## 判定：**一致**

**#500 で返した5件が、この画面では最初から直っています。**
指摘は下のP2が1件だけです。

---

## 見送りの表示（いちばん見たかったところ）

固定データに、選んだルールが条件で見送られた行を2つ入れました。
**どちらも「成功」とは書かれていません。**

| 相手 | 入力 | 画面に出た文 | 印 |
|---|---|---|---|
| 前田 さくら | 予約したい | **1人1回の設定により何もしませんでした** | 何もしませんでした |
| 石田 未来 | こんにちは | **担当者が対応中のため何もしませんでした** | 何もしませんでした |

口の返事も指定どおりです。

```
status        : skipped
domainStatus  : skipped
autoReplyId   : rule-a      ← 選んだルールのまま
detail        : 1人1回の設定により何もしませんでした
```

**理由が行ごとに違う文で出ます。** `SKIP_LABELS` の9つが引かれるので、
「条件に合わず何もしませんでした」の一言に潰れていません。

**「成功」は画面に2回だけ**出ます。実際に成功した2行のぶんです。
一部だけ完了は「一部だけ完了」、返信の失敗は「エラー」、
処理中は「確認待ち」と、**7つの状態が別々の言葉で出ます。**

---

## 未取得「—」と実値0

**分かれています。** 空と失敗を並べると、こうなります。

| | 空（取れて0件） | 失敗（取れていない） |
|---|---|---|
| 今月ヒット・累計・引継ぎ・エラー | `0回` `0回` `0件` `0件` | **`—回` `—回` `—件` `—件`** |
| 稼働状況 状態 | 稼働中 | **—** |
| 優先順位 | 1番目 | **—** |
| 最終実行・平均応答 | **—** / **—** | — / — |
| 要確認 | 確認が必要なエラーはありません。 | **エラー件数を確認できませんでした。** |
| 担当者引継ぎ | `0件` `0件` `0件` | **`—` `—` `—`** |
| 最近の実行 | 実行結果はまだありません | **表示できませんでした** |
| きっかけ別の内訳 | 内訳はまだありません | **内訳を表示できませんでした** |
| CSV・一時停止 | 押せる | **押せない** |

**失敗のときに「0件」も「ありません」も出ません。**
`#500`（リマインダ）はここで通知実績が「送る内容がありません 0通」と
書いていました。**この画面では直っています。**

**空のときでも最終実行と平均応答は `—`** です。
数えるものが無いだけで0ではないので、正しい出し分けです。

きっかけ別の内訳にも見分けが出ます。

```
予約      128回   59.8%
日程変更   54回   25.2%
キャンセル 32回   15.0%
その他      0回      —     ← 回数は実値0、割合は未取得
```

**平均応答は `800ms` → `0.8秒`。** 未取得のときだけ `—`。

---

## 確かめたこと

| # | 何 | 結果 |
|---|---|---|
| 1 | head が `93edbe17` か | **撮る前に確認**（`gh pr view 501`） |
| 2 | 通常・読込・空・失敗 | 4つとも撮れる。`data-list-state` も一致 |
| 3 | 成功・一部失敗・見送り・返信失敗・確認待ち | **5つが別の言葉と色で出る** |
| 4 | ページの横スクロール | **0**（1440・1920とも実測） |
| 5 | 一覧の横スクロール | **0**（内側の要素も実測。`scrollWidth > clientWidth` は0件） |
| 6 | 内部ID | **出ていない**（`friend-N` `are-N` `ml-90NN` を本文で探して0件） |
| 7 | `API error` / `Failed to fetch` | **出ていない**（通常・一時停止の失敗の両方で確認） |
| 8 | 題 | **「予約問い合わせ・実行結果」**。自動応答名が入る |
| 9 | 本文タイトルの重複 | **無い**。`h1` は1つだけ |
| 10 | CSV | **降りてくる。** `auto-reply-runs-rule-a.csv` |
| 11 | 一時停止 | `PUT /api/auto-replies/rule-a` を呼ぶ。405でも**「一時停止できませんでした。状態を読み直してからお試しください。」** |
| 12 | 編集導線 | `/auto-replies/edit?id=rule-a` |
| 13 | 戻り先 | `/auto-replies` |

**失敗のとき、題は「自動応答・実行結果」に落ちます。**
名前が取れないので既定に戻る作りです。正しい振る舞いです。

---

## 返すもの（P2 1件）

### `t7UtYQ` ／ 要確認の文が切れている ／ P2

右の「要確認」の中が **「結果を開いて理由を確認してく…」** で切れます。
1440px・1920px とも同じです。設計は
「Webhook応答を確認してください」で、実装のほうが長い文になっています。

**枠を広げるか、文を短くするかのどちらかです。** 急ぎではありません。

証拠：`t7UtYQ-normal-1440.png` / `t7UtYQ-normal-1920.png`

### 設計との差（記録だけ。直さなくてよい）

| 設計 | 実装 | |
|---|---|---|
| 「返信＋タグ追加」「返信＋担当通知」「担当者へ引継ぎ」 | 「返信」 | `actionSummary` に `{reply:1, tag:1}` が入っているので出せる。**出したほうが分かりやすい** |
| 担当者引継ぎの内訳は無い | 確認待ち8／対応中21／完了7 | **実装が足している。良い** |
| 「入力：」の下に印なし | アバターの丸 | 差なし |

---

## #500 との比べ

**#500 で返した5件のうち、4件はこの画面では起きていません。**

| #500 で返したこと | #501 |
|---|---|
| 失敗したのに「ありません 0件」と書く | **直っている** |
| 1440px で列が切れる | **起きない**（表ではなく行の並びにした） |
| 友だち名の下に内部ID | **出ていない**（「入力：予約を変更したい」になっている） |
| 画面名に名前が入らない | **入っている** |
| きっかけが番号だけ | **入力の文が出る** |

**#500 側を直すときは、この画面を手本にできます。**
