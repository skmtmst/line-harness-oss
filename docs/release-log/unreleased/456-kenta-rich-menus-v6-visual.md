# 機能12 リッチメニューを設計と並べる

V6設計の機能12（9枚）を実装と並べた。5枚が撮れて、4枚は未実装。#456

- どれが出るかを決める「順番」が画面に出ていない。並び順の既定はタップ数が多い順
- targetingPriority を持っているのに、一覧が一度も出していない
- 切替メニューのつながりが図にならない。戻れないことに気づく場所が無い
- publishingAt を持っているのに「予約中」の状態を出せない
