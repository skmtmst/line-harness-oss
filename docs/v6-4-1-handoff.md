# 4-1 友だち属性 — 基準画面の引き渡し

- 作成: 2026-08-26
- 枝: `codex/kenta-v6-visual-qa`
- 更新: 2026-08-26（4状態・モック5ルートを追加）
- 正本: Pencil `★ V6 4-1 友だち属性・タグ（1920）` `hqrOv`
- 併読: `docs/v6-shell-contract.md`（枠の決めごと）、`docs/v6-common-rules.md`（設計の決めごと）

## 0. これは何か

**設計と画像で一致させた最初の1枚。** ここで型を決めて、残り259枚へ広げる。

1枚も一致していない状態で260枚に手を付けると、「バーだけ揃った画面」が
260枚できる。それは進捗に見えて、完成には近づかない。

---

## 1. Codexへの依頼：APIが3つ足りない

**3つとも同じ性質。「そのタグがどこで、どう使われているか」を数えて返す口が
1つあれば全部埋まる。** 推測で埋めていないので、いまは空欄か2値に落ちている。

| 列 | 設計が出すもの | いま | 要るもの |
|---|---|---|---|
| **自動付与のもと** | EC連携／LINE Login／回答フォーム／EC購入／手動／誕生日ルール（6種） | 連動の有無から2種だけ | タグごとの付与元 |
| **使用先** | `配信3・フォーム1` `シナリオ2` `自動応答1` `保存検索2` | `—` | 使われている場所と数 |
| **連動の「他N」** | 灰色の札 `他1` `他3` | 出していない | マイル以外の連動アクション数 |

### 提案する形

`GET /api/tags` の各行に足す。別の口にすると、一覧のたびにN+1回叩くことになる。

```ts
type Tag = {
  // …既存
  /** 付与のきっかけ。取れないときは省く（'manual' で埋めない）。 */
  assignSource?: 'ec' | 'line_login' | 'form' | 'ec_purchase' | 'manual' | 'birthday'
  /** 使われている場所と数。0件の種類は入れない。 */
  usedIn?: { broadcasts?: number; forms?: number; scenarios?: number; autoReplies?: number; savedSearches?: number }
  /** マイル以外の連動アクションの数。 */
  otherActionCount?: number
}
```

**取れないものを既定値で埋めない。** `0` と「取れていない」は別。
`他0` と出すと、設定してあるのに無いように見える。

### 入れる場所

- `apps/web/src/components/friend-fields/tags-page-v4.tsx` の
  `sourceLabel()` / `usageLabel()` / `linkChips()` に1行ずつ。
  **どこに何を足すかはコメントで書いてある。**

---

## 2. 残り259枚へ広げられる型

**4-1で確かめた。ほかの機能でも同じにする。**

| # | 決めごと | 根拠 |
|---:|---|---|
| 1 | **本文にタイトルと説明を置かない。** バーが持つ | `v6-shell-contract.md` §2 |
| 2 | **ヘッダー操作は独立した行にしない。** 本文の1行目（タブ行・パンくず行・案内帯）の右へ寄せる | `v6-common-rules.md` §1-4 |
| 3 | **作る操作は左。** `フォルダを追加` `＋◯◯を作成` はKPIの下、左寄せ | 同 §1-5 |
| 4 | **表の操作列は赤いゴミ箱だけ。** 「編集」の文字を置かず、対象名から開く | 設計 `E2NC4` |
| 5 | **並び替えのつまみは常に出す。** ボタンで出し入れしない | 設計 `i1Xb2V`。押す前に気づけない |
| 6 | **一覧の数はサーバーから。** 画面で数えない | 下記 §3 |
| 7 | **プルダウンは `.v6-select`** を付ける | 素の矢印は枠にくっついてはみ出す |
| 8 | **表が詰まったら表の中だけ横スクロール。** 画面ごと伸ばさない | 同 §1-8 |
| 9 | **共通部品を探してから書く。** ページ送り・タブ・ボタンは設計と一致した部品が既にある | 下記 §3 |

### 使う共通部品

| 設計 | 部品 |
|---|---|
| `Blot6` ページ送り | `shared/pagination.tsx` |
| `VPn1F` / `ISA1Q` タブ | `shared/tabs.tsx`（`actions` で右端に操作を置ける） |
| `uzNEC` / `nBRKk` ボタン | `shared/button.tsx` |
| `XywGr` サマリーカード | `shared/list-kpis.tsx`（`/api/list-stats` から取る） |
| `cBSCb` トップバー | `shell/app-top-bar.tsx`（画面ごとに置かない） |

### 画面名を渡す

**ルートからは決められない。** ★V6 260枚を数えたところ、メニュー名で足りるのは
約120枚だけ。約100枚が固有名、約40枚がデータの中身そのもの
（`Kenta Kawano(Obama)` `夏の定番セット.jpg` `ももちゃん ／ トリミング`）。

```tsx
import { usePageTitle } from '@/components/shell/page-chrome'

usePageTitle(friend?.displayName ?? null)  // 読み込み中は null。空文字を渡さない
```

受信箱のような全画面は `useFullWidthPage()` を呼ぶ。**ルート名で判定しない。**

---

## 3. 4-1で見つかった不具合（ほかの画面にも同じものがあるはず）

**画像で並べたから見つかった。テストは全部通っていた。**

| 不具合 | 中身 | ほかも見るべきか |
|---|---|---|
| `--text-title`(20px) が未定義 | トップバーが `var(--text-title)` を読んでいたが変数が無く、画面名が16pxに落ちていた。**CSSは未定義の変数を黙って無視する** | **要**。`var(--…)` が全部定義ずみか一度洗う |
| `--radius-mini`(6px) が未定義 | 行の中の札の角丸が書けなかった | 同上 |
| 「付与済み友だち」の数え方 | タグごとの人数を**足していた**。2つタグが付いた人を2人と数える | **要**。ほかの一覧も画面で数えていないか |
| 設計のフォントが2系統 | Noto Sans JP 6,753 と Inter 4,565 が混在。Inter は共通関数の既定値が入り込んだもの | 済。設計側を統一した |
| 共通部品があるのに自前で組んでいた | ページ送りは設計と一致した部品が既にあった | **要**。ほかの一覧も同じはず |

---

## 4. 画面を見る手順

**これが無いと、設計と比べられない。** ローカルで開くのに4つ要る（`v6-shell-contract.md` §10）。

```bash
# 1. 共有パッケージを建てる（無いと全ルート500）
pnpm --filter "@line-crm/shared" --filter "@line-crm/line-sdk" --filter "@line-harness/update-engine" build

# 2. Workerの代わり（ローカル専用・GET以外は405・固定データ）
pnpm qa:mock

# 3. 管理画面
pnpm qa:web

# 4. 画像で比べる（1440・1920）
pnpm qa:shot
```

- `scripts/visual-qa/mock-api.mjs` … 本番では起動しない。更新は必ず失敗する
- `scripts/visual-qa/fixtures.mjs` … **Pencil の絵に書いてある値そのまま**。
  タグ101件・フォルダ6つ・KPI 4枚。**6件だけで比べない**（桁あふれも折り返しも
  ページ送りも出ず、「一致した」と言えてしまう）
- `scripts/visual-qa/routes.mjs` … 見張るルート。いま**12ルート × 1440/1920 ＋ 4-1の4状態 × 2 = 32件**。
  `NOT_READY` は空（ダッシュボード・分析・一斉配信・リッチメニュー・
  オートメーションの5つを直して入れた）。
  増やすときは、そのルートが本当に描けることを手で1回確かめてから足す
- `scripts/visual-qa/api-shapes.mjs` … 配列で返る口を `api.ts` から読む。**手で並べない**

**初回は基準の画像が無いので全件「失敗」になる。** 撮り終えてから
もう一度走らせると通る。基準画像は `capture.spec.mjs-snapshots/` に入る。

### 見張っている4つ

1. **そのURLに居るか。** `AuthGuard` は `router.replace('/login')` で飛ばすので、
   エラーの文言はどこにも出ない。行き先のURLを見ないと、ログイン画面を
   撮って「通過」になる（実際に24件そうなった）
2. 描けているか（「画面を表示できませんでした」「店舗が選ばれていません」が出ていない）
3. **1440px と 1920px の両方で横スクロールが出ていないか**
4. 前回の画像と変わっていないか

### 違ってよい点の数は **0**

割合（`maxDiffPixelRatio`）にすると、縦に長い画面ほど甘くなる。
1920pxの1枚で 0.2% は約4,000点あり、**開発表示の丸い印（3,804点）が
丸ごと通った**。数に変えても120では「が」を「は」に変えた1文字（約70点）が
通ってしまう。乱数も時刻も使わず、開発表示は撮る瞬間に消す
（`screenshot.css`）ので、同じ絵はそっくり同じになる。**0で3回続けて通る**ことと、
**1文字変えると落ちる**ことの両方を確かめてある。

### 落ちたときに多いもの

**`◯◯.filter is not a function`** ／ **`undefined の ◯◯ が読めない`。**
モックの返す形の取り違え。配列で返る口は `api.ts` から自動で読んでいるので
**手で足さない**。足りないのは器のほう（`mock-api.mjs` の `SHAPES`）。
**推測で足さない。落ちた画面のコンソールを見て、その口の形だけを足す。**

---

## 5. 4-1固有（横展開しないもの）

- フォルダの箱 240px（設計も240へ変えた）
- タグ101件・フォルダ6つの母数
- 連動の札3色（緑＝マイル／黄＝倍率／灰＝その他）

---

## 6. 4状態（設計 ★V6 4-2-C `yKEdO`）

**ふつうの状態だけ合わせて「一致した」と言わない。** 4つを別々の画像で見張る。

| 状態 | 出るもの | 設計の部品 |
|---|---|---|
| 読込 | 輪 ＋「読み込んでいます」 | `sZE9Q` |
| 空（1件も無い） | 受け皿 ＋「まだタグがありません」 | `BDOJu` |
| 空（絞り込みに合わない） | 受け皿 ＋「条件に合うタグはありません」 | `BDOJu` |
| エラー | 赤い三角 ＋「表示できませんでした」 | `OLMp0` |
| 権限不足 | 錠 ＋「表示する権限がありません」 | **設計に部品が無い** |

- 共通部品は `components/shared/list-state.tsx`。高さ132・`$surface-pearl`・
  `$radius-md`・padding 24・gap 8・中央寄せ。**設計の実測値そのまま**
- **権限不足の絵は設計に無い。** 設計は「権限不足とエラーは今までどおり」と
  しか決めていない（`v6-design-review-handoff.md`）。`BDOJu` と同じ枠のまま
  絵と文言だけ変えている。**専用の絵が描かれたら合わせ直す**
- 403 は `error` と分ける。「壊れた」と出すと、直らない再読み込みを繰り返させる
- **中身を出せていないあいだは数を出さない。** フォルダの件数も、
  「1〜20 / 101件」も、ページ送りも `—` か非表示。0件と出すと消えたように見える
- 権限が無いときは「フォルダを追加」「＋ タグを追加」「CSVで一括登録」を出さない

画像は `tags-loading` / `tags-empty` / `tags-error` / `tags-forbidden` の
各1440・1920。差し替えは**ブラウザ側だけ**で、モックは触らない
（触ると他の画面の画像まで変わる）。

---

## 7. まだやっていないこと

1. 上の3つのAPI（`assignSource` / `usedIn` / `otherActionCount`）
2. 残り259枚への横展開（**4-1のPRが取り込まれてから**）
3. KPI 4枚は、数が取れないと見出しだけ残して「—」になる。
   `ListKpis` に `titles` を渡した画面だけ。ほかの4画面はまだ見出しも消える
