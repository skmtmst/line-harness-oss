# 開いているPRの扱い（2026-08-16時点の記録）

この文書は **2026-08-16 時点の整理記録**。現在のPR状態を示すものではない。
最新の状態はGitHubの各PRで確認すること。

当時、**0.23.0 と v0.24.0 は `codex/development` にマージ済み**だった。

当初は26本の積み上げだったが、系譜を調べたところ
**0.23.0 の9本と v0.24.0 の14本はすべて `codex/kenta-v024-deferred` の祖先**だった。
先端1本（#58）をマージすれば全部入るので、そうした。
取り込み済みの20本は閉じてある（差分は失われていない）。

---

## 2026-08-16時点で入っていたもの

| PR | 中身 |
|---|---|
| #59 | gate を全PRに掛ける（積み上げPRが未検査だった） |
| #61 | マイグレーションをゲートの内側に入れる |
| #58 | 0.23.0 と v0.24.0 の全部（203ファイル） |
| #63 | `bootstrap.sql` とのずれを埋める（105） |

マイグレーションは **088〜105 の18件**。

---

## 2026-08-16時点で残っていたPR

| PR | 中身 | 状況 |
|---|---|---|
| #38 | broadcasts テストのスキーマを `bootstrap.sql` から組み立てる | 当時は唯一の未反映。現在は本体と差分ゼロを確認してクローズ済み |
| #62 | `pnpm` が無いPCでの手順 | 文書のみ |
| #60 | この文書 | 文書のみ |
| #33 | 反映履歴の設計案 | 当時は質問が5件あった。現在は決定事項へ更新してマージ済み |
| #1 | リッチメニュー登録エラーを具体化 | base が `main`。他と独立 |

順番の制約は無い。どれから見てもよい。

---

## 検証環境の状態

**マイグレーションは適用済み。** 2026-08-16 に実施。

```
適用済み 102 / 未適用 0 / 部分 0 / 判定不能 7
_migrations に記録済み 109 件
```

当てたのは33件。今回のぶん16件（088〜103）に加えて、
**もともと空いていた穴10件**（051・057〜062・064〜066）と
データのみ7件（026 034 039 063 080 081 104）。

穴はマイル機能・ウェビナー追客・面談リマインダ。
検証・本番の両方で当たっていなかった。

復元点（30日間有効）:

```
000003a0-0000000c-000050c9-8368f2449a27ce1a2ed78a7482a20a6d
```

105 はこのあと当てる。

---

## 本番の状態

**まだ何も当てていない。** 適用は masato の判断。

本番は検証より遅れている。

| | 検証 `nen-line-stg` | 本番 `nen-line` |
|---|---|---|
| 適用済み（適用前） | 76 件 | 71 件 |
| 未適用（適用前） | 26 件 | 30 件 |
| 部分適用 | 0 件 | **1 件（077）** |

**本番だけ 084〜087 も当たる**（スタッフの閲覧専用権限、流入経路のジャンル、
配信メッセージの組み立て）。確認項目が検証より多い。

**077 が部分適用なので、`migrate.ts` は止まる。**
先に中を見て、人が判断すること。

手順は `docs/migration-runbook.md` にある。
