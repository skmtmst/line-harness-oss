# 設計を決めないといけないところ（v0.25.0）

V2設計どおりに作れなかった箇所の一覧。

実装は止めずに進めている。**設計にある数字を出す仕組みが無いとき、
数字を捏造せず、意味の近い別の数を出すか「—」にしている。**
どちらを選んだかと、設計どおりにするなら何が要るかをここに書く。

- 更新: 2026-08-16
- 対象: `docs/design-node-ids.md` の V2 85画面

---

## 見方

| 列 | 意味 |
|---|---|
| 重さ | **大**＝新しい表かマイグレーションが要る／**中**＝集計の実装が要る／**小**＝既存データの読み替えで済む |
| いまの実装 | 設計どおりでない場合に、代わりに出しているもの |

---

## 1. ダッシュボード（`EgKGw`）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 1-1 | 平均の初回返信（18分） | 最も古い未対応の経過時間 | 受信→初回返信の所要時間を記録する。`chats` に `first_replied_at` を足すか、`messages_log` から都度計算する | 大 |
| 1-2 | 友だち数の推移「流入元の内訳」（Instagram 1・店頭POP 1） | 「—」 | 日次で `entry_routes` 別に数える。`friend_daily_snapshots` に経路別の行を足すのが素直 | 大 |
| 1-3 | ケアが必要な子（モカ／ソラ／ミケ、便がやわらかい 4日連続） | **未実装** | `nen-members` 側の健康記録から連続検知を出す口。`/api/nen-members/overview` に近いものがある | 中 |

## 2. 受信箱（`nR2FI`）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 2-1 | 平均の初回返信（18分・過去7日） | 1時間以上待たせている件数 | 1-1 と同じ | 大 |
| 2-2 | 一覧の「返信待ちのみ」絞り込み | 既存の状態フィルタのみ | 「最後が受信で、その後に送信が無い」を判定する。`unanswered-inbox.ts` に近い実装がある | 中 |

## 3. 友だち（`P2guNv`）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 3-1 | 「今月の追加」の前月比（+3） | 前月の実数を併記 | 設計は増減だけ。月初は必ずマイナスに見えるので、実装では前月の実数も出している。**設計を実装に寄せるか要判断** | 小 |
| 3-2 | 保存した検索のチップ（未対応かつ7日以内 ほか） | 未実装 | `saved_searches` は 100 で入っている。一覧画面から呼ぶ導線が無いだけ | 小 |
| 3-3 | 一覧の「★つきタグ・友だち情報」列 | 未実装 | タグに「★つき」の印が無い。`tags` に列を足すか、表示順の上位N件を出すか | 中 |

## 4. 一斉配信（`Jv6hR`）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 4-1 | 今月の残枠（4,991 / 上限 5,000） | 失敗件数 | LINEの `message/quota` はダッシュボードで実装済み。一覧のためだけに外部APIを叩くのが重いので出していない。**叩いてよいなら小** | 小 |
| 4-2 | 平均開封率 | 実装済み。ただし20人未満の配信を平均から除外 | LINEは20人未満だと開封数を返さない。0として混ぜると平均が不当に下がるため除いている。**この扱いでよいか要確認** | — |
| 4-3 | フォルダ（01_お知らせ／02_キャンペーン／未分類） | 未実装 | `folders` は 099 で入っている。配信に `folder_id` が無い | 中 |

## 5. タグ／友だち属性（`IvqXE`）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 5-1 | 自動付与ルール（7件・自動応答・フォーム由来） | 未使用のタグ | 自動応答とフォームの「タグを付ける」設定を横断して数える口 | 中 |
| 5-2 | 「付与済み友だち 全体の 100%」 | 実数のみ | 母数をどれにするか（有効友だち／総数）で数字が変わる。**要判断** | 小 |

## 6. テンプレート（`YTMBU`）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 6-1 | 平均クリック率（12%・短縮URL経由） | 未使用の件数 | 短縮URLのクリックをテンプレート単位に集計する。`tracked_links` とテンプレートの紐づけが無い | 大 |
| 6-2 | 今月の送信（2,180通） | 全送信数 | テンプレート経由だけを切り分ける印が `messages_log` に無い | 中 |
| 6-3 | 未使用「90日以上」 | 参照されていない数 | 最終使用日を持っていない。参照の有無しか見ていない | 中 |

## 7. シナリオ（`o0oT6d`）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 7-1 | 今週の配信（1,204通・前週比 +6%） | 未実装（4枚目は稼働中） | シナリオ由来の送信を切り分ける印が `messages_log` に無い。6-2 と同根 | 中 |

## 8. リマインダ（`g4I4C`）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 8-1 | 起点になる項目（3件・友だち情報欄） | 止めている件数 | `friend_fields` に型の区別が無く、日付型を数えられない。`type` 列を足すのが素直 | 中 |
| 8-2 | 今月の配信「到達率 99%」 | 未実装 | リマインダ由来の送信を切り分ける印が無い。6-2 と同根 | 中 |

## 9. 対応マーク（`rKbqo`）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 9-1 | 自動で変わるルール（3件・受信時・返信時） | 「対応済」の累計 | 受信時・返信時にマークを変える設定を数える口。`support_marks.auto_on_inbound` はあるが、返信時のルールが無い | 中 |
| 9-2 | 「対応済 26人・過去7日」 | 累計 | マークが変わった日時を記録していない。`friends.support_mark_id` は現在値だけ | 中 |

## 10. 保存した検索（`HdEpm`）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 10-1 | 配信で使用中（6件・一斉配信/シナリオ） | 「空き」件数 | 一斉配信やシナリオが保存した検索を参照する仕組みが無い。いまは条件をその場で書く | 大 |
| 10-2 | 該当者が0人（2件・条件の見直しを推奨） | 未実装 | 保存した条件を実際に流して件数を数える必要がある。12件ぶん毎回走らせると重い。日次で数えて持つのが現実的 | 中 |
| 10-3 | 今月の呼び出し（48回） | 未実装 | 呼び出し回数を記録していない | 中 |
| 10-4 | 上限50 | 画面に出しているだけ | DB側に制約は無い。増えすぎたら保存時に止める話 | 小 |

## 11. 受信箱の細部（`nR2FI`／HTML書き出しで判明）

設計を HTML に書き出して1語ずつ突き合わせたら、部分読みでは見えなかった
ものが出た。**この方法が一番正確。以降はこれで確認する。**

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 11-1 | 「表示：受信・送信・システム」の絞り込み | 枠だけ（押せない） | トークに出すものを種類で絞る。システムイベントを隠せると読みやすい | 中 |
| 11-2 | 友だち詳細の「流入元 SNS」 | 「不明」固定 | `friends.first_tracked_link_id` はあるが、この経路で引いていない | 小 |
| 11-3 | 「★つき友だち情報」 | 登録の先頭3件 | `friend_fields` に「よく見る印」が無い。列を1つ足せば済む | 小 |
| 11-4 | Head の「マニュアル」 | 押せない | 行き先の文書が無い。仮のリンクを置くと行き止まりになる | 小 |
| 11-5 | 「すべて確認済みにする」 | 確認だけ出て何もしない | 一括で既読にする口が無い | 中 |

## 12. ダッシュボードの細部（`EgKGw`／HTML書き出しで判明）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 12-1 | ケアが必要な子（警告帯＋右カラム） | **何も出さない** | 健康記録の連続検知。仕組みが入るまで帯を空にした。適当な警告を出すより良い | 中 |
| 12-2 | 出荷予定のタブ「今週 / 遅延」 | 今日・明日 / あさって以降 | 遅延を出すには出荷済みかの判定が要る | 中 |
| 12-3 | 出荷予定の「数量」列 | 「—」 | `ec_events.payload` にはあるが、出荷予定のAPIが返していない | 小 |
| 12-4 | 今月の配信「プッシュ数 / リプライ数」 | 送信数 / 一斉配信 | LINEは自発と応答で課金が違う。`messages_log` に区別が無い（§A と同根） | 中 |
| 12-5 | 直近の成果「初回購入 / 定期便 申込 / 承認待ちの成果」 | 成果地点ごとの上位5件 | 設計の3つは特定の成果地点名。環境ごとに違うので、上位5件を出している。**設計を実装に寄せるか要判断** | 小 |
| 12-6 | 友だち数の推移「流入元の内訳」 | 「—」 | §1-2 と同じ |

## 13. 友だち画面の細部（`P2guNv`／HTML書き出しで判明）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 13-1 | 「最終接触」列 | 登録日を出している | 最後の受信時刻を一覧のAPIが返していない。`chats.last_incoming_at`（107で追加）を join すれば出る | 小 |
| 13-2 | 「★つきタグ・友だち情報」列 | タグと流入を出している | タグに「★つき」の印が無い（§3-3 と同じ） | 中 |
| 13-3 | 保存済みチップ（未対応かつ7日以内 ほか） | 押せない枠 | `saved_searches` はあるが、条件を一覧に適用する口が無い | 中 |
| 13-4 | 一括操作6種 | 押せない枠 | 選択の仕組みと、6種それぞれの実行口が要る | 大 |
| 13-5 | 詳細検索 / 保存した検索 | 押せない枠 | 条件を組み立てる画面が要る | 大 |
| 13-6 | CSVで書き出す | 押すと案内だけ | 書き出しの口が無い | 中 |
| 13-7 | タブの位置と呼び名 | 画面の一番上（設計はKPIの下）。「統合ユーザー」（設計は「アカウント横断の名寄せ」） | `MergedTabs` の置き場所を変える。8画面が使っているので影響を確かめてから | 中 |

**13-4 と 13-5 が大きい。** どちらも「選ぶ」「条件を組む」という仕組みそのものが要る。

## 14. メールと友だちが紐づいていない

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 14-1 | メールを開いても友だち詳細が出る | 「紐づいていません」と案内 | `support_email_threads` は `customer_email` しか持たない。メールアドレスから友だちを引く口が要る。`friends` にメールアドレスの列も無い | 大 |

EC連携で会員のメールアドレスは持っているので、そこを経由すれば繋がる。
ただし1人が複数のアドレスを使う場合の扱いを決める必要がある。

## 15. 一斉配信の細部（`Jv6hR`／HTML書き出しで判明）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 15-1 | フォルダ（01_お知らせ / 02_キャンペーン / 未分類） | 枠だけ（押せない） | `folders` は 099 で入っているが `broadcasts.folder_id` が無い。1列足せば済む | 小 |
| 15-2 | 並び順「配信日が新しい順」 | 枠だけ | 一覧のAPIが並び順を受け取らない | 小 |
| 15-3 | 保存した条件（よく使う / 予約中のみ / 開封率が低い / 今月分） | 枠だけ | 一覧の絞り込み条件を保存する仕組みが無い（§13-3 と同根） | 中 |
| 15-4 | 「開封（率）」列 | 実績の中に混ぜて出している | 列として独立させた。開封が取れていない配信は空になる（20人未満はLINEが返さない） | — |

## 16. テンプレートの細部（`YTMBU`／HTML書き出しで判明）

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 16-1 | フォルダ（01_定期便 / 02_健康フォロー / 未分類） | 枠だけ | `templates.folder_id` が無い（§15-1 と同じ） | 小 |
| 16-2 | 「使われている配信」列 | 使用数のみ | どのシナリオ・配信から参照されているかを名前で出す。参照元を辿る口が要る | 中 |
| 16-3 | 一覧のカルーセル / 変数ありの絞り込み | 種別の絞り込みのみ | 本文に `{{...}}` が入っているかで判定できる。実装は容易 | 小 |
| 16-4 | 並び順「使用回数が多い順」 | 枠だけ | 一覧のAPIが並び順を受け取らない（§15-2 と同じ） | 小 |

## 17. シナリオとリマインダの細部（`o0oT6d` `g4I4C`／HTML書き出しで判明）

どちらも設計は**表**だが、実装はカード。表に替えるには行の中身を
組み直す必要がある。今回はフォルダ・検索・並び順の帯までを揃えた。

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 17-1 | シナリオの一覧が表（名前 / 購読中 / 読了 / 最終ステップ後 / 状態） | カード | 行の組み直し。「最終ステップ後」（次のシナリオへ / 配信可）は列が無い | 中 |
| 17-2 | リマインダの一覧が表（リマインダ名 / 起点になる日付 / 配信タイミング / 状態） | カード | 同上。「起点になる日付」は 092 の `reminders.trigger_type` で出せる | 中 |
| 17-3 | フォルダ | 枠だけ | `scenarios.folder_id` は 099 で入っている。`reminders.folder_id` も。**絞り込む口だけが無い** | 小 |
| 17-4 | 並び順（購読中が多い順 / 配信待ちが多い順） | 枠だけ | 一覧のAPIが並び順を受け取らない（§15-2 と同じ） | 小 |
| 17-5 | 保存した条件（停止中のみ / 離脱が大きい / 今月作成 ほか） | 枠だけ | §13-3 と同根 | 中 |

**17-3 は他の画面と違い、列が既にある。** 099 で `scenarios.folder_id` と
`reminders.folder_id` を足してある。絞り込みの口を作れば動く。

## 18. 友だち属性4タブの細部（`IvqXE` `KWqHd` `rKbqo` `HdEpm`／HTML書き出しで判明）

253種のうち実装に無いものが136件。これまでで最大。設計はどのタブにも
「この画面が何をする場所か」を書いており、そこが丸ごと無かった。
説明と使い方の案内は今回入れた。残りは表の中身。

| # | 設計 | いまの実装 | 要るもの | 重さ |
|---|---|---|---|---|
| 18-1 | タグの一覧が表（タグ名 / 友だち人数 / 自動付与のもと / 一覧に表示） | カード | 「自動付与のもと」（CV「定期便申込」／フォーム「初回カウンセリング」／健康記録の連続検知）を出すには、付与の由来を記録する必要がある | 大 |
| 18-2 | 友だち情報欄の表（項目名 / 種別 / 既定値 / 入力元 / 入力済み） | 一覧 | 「種別」（単一行 / 電話番号 など）は `friend_fields` に型が無い（§D）。「入力元」はどのフォームから入ったかの記録が要る | 大 |
| 18-3 | 対応マークの「自動で切り替える」ルール | 無し | 受信時・返信時・自動応答時にマークを変える設定。`support_marks.auto_on_inbound` はあるが、返信時と自動応答時が無い | 中 |
| 18-4 | 保存した検索の条件の中身（すべて満たす / いずれか1つ以上） | 一覧のみ | 条件を組み立てて保存する画面が要る（§13-5 と同根） | 大 |
| 18-5 | 「どこで使われているか」（受信箱の絞り込み ほか） | 無し | 参照元を辿る仕組み。§16-2 と同根 | 中 |
| 18-6 | フォルダ（01_購入ステータス ほか） | 枠だけ | `tags.folder_id` は 099 で入っている。絞り込む口だけが無い（§17-3 と同じ） | 小 |

---

## まとめ：根っこは5つ

個別に見ると19件あるが、原因は5つに集約される。

### A. 送信の出どころを記録していない（6-2 / 7-1 / 8-2）— **誤り。028 で既にあった**

`messages_log` は 028 の時点で `source` を持っている。

  user / broadcast / scenario / auto_reply / reminder / manual

さらに 038 で `template_id_at_send`（送信時に使ったテンプレート）もある。

**「記録していない」は私の思い込みだった。** 実物を確かめず決めつけていた。

したがって次はすべて**読み出しを書くだけ**で済み、解決した。

  テンプレートの「今月の送信」  template_id_at_send IS NOT NULL で絞る
  シナリオの「今週の配信」      source = 'scenario'
  リマインダの「今月の配信」    source = 'reminder'
  プッシュ数 / リプライ数       source IN ('auto_reply','manual') が応答

108 で origin_kind / origin_id を足してしまった。検証環境に入っており、
追加のみのポリシーで消せない。**どこからも読み書きしない。**

### B. 初回返信の時間を記録していない（1-1 / 2-1）— **107 で解決済み**

受信してから最初に返すまでの時間。`chats` に `first_replied_at` を足し、
送信時に「まだ入っていなければ入れる」だけでよい。

### C. 短縮URLとテンプレートが紐づいていない（6-1）— **110 で列を用意した**

`tracked_links` はテンプレート本文から自動生成されるが、どのテンプレート由来かを
持っていない。クリック率をテンプレート単位で出すにはこれが要る。

### D. 友だち情報欄に型が無い（8-1）— **誤り。099 で既に入っていた**

`friend_fields` は 099 の時点で `type`（text / textarea / number / date /
select / multi_select / checkbox / url / tel / email）と `is_starred` を
持っている。

**「型が無い」は私の思い込みだった。** 突き合わせ表を書いたときに実物を
確かめず、そう決めつけた。109 を足そうとして migrate.ts に止められ、
実物を見て気づいた。

したがって 8-1（リマインダの「起点になる項目」）と 18-2（情報欄の「種別」列）は、
**列を足す必要が無い**。`type = 'date'` の項目を数え、`type` をそのまま
出せばよい。読み出しを書くだけ。

109 はどの環境にも当たっていない。何もしないファイルとして残してある。

### E. 「いつ変わったか」を残していない（9-2 / 10-2 / 10-3）— **110 で表を用意した**

対応マークも保存した検索も、いまの値しか持っていない。設計は
「過去7日」「今月の呼び出し」のように期間で切って見せる。

個別に履歴表を足すより、汎用の操作記録を1つ持つほうがよい。
`login_audit`（103）と同じ形で、対象・種類・日時を残す。

---

## 根っこの状況（2026-08-16 更新）

| | 根っこ | 列・表 | 書き込み | 読み出し |
|---|---|---|---|---|
| A | 送信の出どころ | **028 で既にあった** | 済 | **済**（4か所とも） |
| B | 初回返信の時間 | 107 | **済** | **済**（平均の初回返信） |
| C | 短縮URLとテンプレート | 110 | **未** | 未 |
| D | 友だち情報欄の型 | **099 で既にあった** | 済 | **未** |
| E | 操作の記録 | 110 | **未** | 未 |

**B は端まで通った。** 受信時に `last_incoming_at` を残し、送信時に
`first_replied_at` を入れ、ダッシュボードと受信箱に「平均の初回返信」を出す。

**A・C・D・E は列と表を用意しただけ。** 書き込む処理と読み出しはこれから。
入れ物が無いと着手できないので、先に用意した。

---

## 次に決めてほしいこと

1. **A（送信の出どころ）を入れるか。** 1マイグレーションで3件解ける。
   過去ぶんが埋まらないことを許容できるか
2. **4-2（20人未満の配信を開封率の平均から除く）でよいか。** いまそう実装している
3. **3-1（今月の追加の前月比）** を設計どおり増減だけにするか、実装のとおり
   前月の実数も出すか

1 が一番効く。2 と 3 は見た目の話なので、実物を見てから決めても遅くない。

---

## 予約まわり（V2 8-1〜8-2-4、2026-08-17 に実装）

7画面を設計と突き合わせた。**動く形にはなっている**が、
以下は「どう持つか」を決めないと数字が出せない。

| # | 画面 | 内容 | いま出しているもの |
|---|---|---|---|
| 15-1 | 8-1 予約管理 | **予約経路**の列。`bookings` に列が無い | いま予約はLINE内のフォームからしか入らないので「LINE」固定 |
| 15-2 | 8-1 予約管理 | **変更依頼**という状態。`bookings.status` の CHECK に無い | 承認待ちの件数。要対応なのは同じ |
| 15-3 | 8-1 予約管理 | **予約履歴URL**。友だちが自分の予約を見るLIFFページが無い（`page=` の許可一覧は salon-book / event / event-me / webinar） | 「準備中です」 |
| 15-4 | 8-1 予約管理 | 並び順・表示件数・保存した条件 | 押せない状態で置いてある |
| 15-5 | 8-1-1 予約の詳細 | **連絡先**。電話番号は `bookings` にも `friends` にも無い | 「友だち情報欄で管理しています」 |
| 15-6 | 8-1-1 予約の詳細 | **日時を変更する**。予約の時刻を書き換えるAPIが無い | 押せない。いったん拒否して取り直す |
| 15-7 | 8-1-1 予約の詳細 | **承認時にひとことを添える**。承認APIが本文を受け取らない | 押せない状態で置いてある |
| 15-8 | 8-2 予約設定 | **枠の稼働率**。受付時間の総枠数を数える仕組みが無い | 「—」 |
| 15-9 | 8-2 予約設定 | **予約フォーム**タブ。フォームの見た目を変える画面が無い | 押せないタブ |
| 15-10 | 8-2-2 スタッフ登録 | **顔写真の取り込み**。画像を上げる導線が無い | URLを貼る欄 |
| 15-11 | 8-2-2 スタッフ登録 | **予約枠の色**。`staff` に色の列が無い | 押せない選択欄 |
| 15-12 | 8-2-3 受付時間 | **30分きざみの升目**。「休憩」という考え方が `availability_rules` に無い | 曜日ごとの開始と終了 |
| 15-13 | 8-2-3 受付時間 | **ほかのスタッフにコピー**。上書き先を丸ごと置き換えるので取り消せない | 「準備中です」 |

### いちばん先に決めたいもの

**15-2（変更依頼）と 15-6（日時を変更する）は同じ話。**
お客様が「日時を変えたい」と言ったとき、いまは拒否して取り直すしかない。
予約1件に対して「変更を申し込む」を作るのか、それとも予約を作り直して
古いほうにひもづけるのか。どちらにするかで、`bookings` に足す列が変わる。

追加のみのポリシーで列は消せないので、**決まる前に足さない**。

**15-12（升目）は大きい。** 休憩を持つなら `availability_rules` に
「その日の中の受けない時間」を足すことになる。いまの開始・終了だけの持ち方でも
店は回るので、実際に休憩を挟む運用があるかを先に確かめたい。

## イベントまわり（V2 8-3 / 8-3-5、2026-08-17 に実装）

| # | 画面 | 内容 | いま出しているもの |
|---|---|---|---|
| ~~16-1~~ | 8-3 一覧 | ~~**申込条件**~~ | **完了**（タグ名を出す） |
| 16-2 | 8-3 一覧 | フォルダ・並び替え・保存した条件 | 押せない状態で置いてある |
| 16-3 | 8-3-5 予約者 | **キャンセル待ちの人数**。`events.waitlist_enabled` はあるが、待っている人を数える場所が `event_bookings` に無い | 「—」＋受け付ける設定かどうか |
| 16-4 | 8-3-5 予約者 | **同伴をふくむ人数**（会場の準備人数）。同伴の人数を持つ列が無い | 出していない |
| 16-5 | 8-3-5 予約者 | **予約者に一斉送信**。配信の宛先はタグか友だち全体で、イベントの申込者を宛先にできない | 押せない |
| 16-6 | 8-3-5 予約者 | **まとめて操作**（承認する / 却下する / メッセージを送る / タグを付ける） | 1件ずつの操作のみ |
| 16-7 | 8-3-4 公開設定 | **公開する日時**（予約公開）。`events` に公開予定時刻の列が無い。`is_published` の 0/1 だけ | 「すぐに公開する」を押せない状態で置いてある |
| 16-8 | 8-3-4 公開設定 | **主催者にもメールで知らせる**。宛先を持つ列も、送る仕組みも無い | 押せない状態で置いてある |
| 16-9 | 8-3-2 概要 | 画像を選ぶ帯の文言が設計と違う（実装「画像を選択 / またはドラッグ&ドロップ」、設計「画像を選ぶ / ドラッグ＆ドロップ、または Cmd+V でも貼り付けられます」）。`image-uploader.tsx` は全画面で共有 | 実装の文言のまま |

**16-1 は完了した（2026-08-17）。** ただし前提が1つ間違っていた。

一覧APIは `SELECT e.*` なので `visible_tag_id` は**もとから返っていた**。
足りなかったのはタグ名のほうで、ID だけでは一覧に文字を出せなかった。
名前を引くサブクエリを足して解決した。

ここで1つ分かったことがある。**タグを消しても `events.visible_tag_id` は残る。**
その状態のイベントは、公開していても LIFF の一覧に誰にも出ない。
「全員」と同じ見た目にすると気づけないので、一覧では「消えたタグ」と出している。
（`events.visible_tag_id` に外部キーが無いため。追加のみのポリシーで後付けもできない）

### 手をつけていないイベント画面

| 画面 | node | 状態 |
|---|---|---|
| 8-3-1 イベントの編集 | `Ks5oq` | `event-form.tsx` のタブのまま。設計と突き合わせていない |
| ~~8-3-2 イベントを作る ①概要~~ | `C2Qr4h` | **完了**（2026-08-17） |
| ~~8-3-3 イベントを作る ②予約枠~~ | `u9Dp8` | **完了**（2026-08-17） |
| ~~8-3-4 イベントを作る ③公開設定~~ | `G1lc3` | **完了**（2026-08-17） |

作成の3段階は `event-wizard.tsx` に分けた（`/events/new?step=1..3`）。
`event-form.tsx` のタブは編集（8-3-1）が使い続けている。**枠の追加まわりが
両方に書かれている状態**なので、8-3-1 を設計に合わせるときに片方へ寄せること。

②の一括追加は、設計が「時間帯＋1枠の長さ」で指定するのに対し、
`generateBulkSlots` は時間帯の組を受け取る。間を `splitBand`（`jst.ts`）が
埋めていて、割り切れない端の枠は作らない。
